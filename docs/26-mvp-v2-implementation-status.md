# Pagnetic MVP v2 — implementation status

Development update (2026-09-06T18:52Z): the owner-authorized adaptive scope in [doc60](./60-adaptive-storefront-prd.md) is implemented through AP-06 and safely deployed. The final pinned Node 24 gate passed 358/358 tests plus TypeScript, ESLint and application/Shopify builds. Fly machine version 24 is healthy, all 28 migrations are current, a fresh encrypted off-volume backup/readback/restore passed, and Shopify release `pagnetic-ap06-20260906` is live. Terms date `2026-09-06` and the encrypted Make alert endpoint are configured; production readiness passes 47/47. Billing, v2 treatment, shadow, model and offer publication remain off. Actual email receipt, distribution review and design-partner content/QA gates remain external. Exact evidence is in [doc64](./64-adaptive-development-status.md) and [doc65](./65-adaptive-storefront-release-checklist.md).

Created: 2026-09-05  
Authority: [executable PRD](./24-pagnetic-mvp-v2-executable-prd.md)  
Operator instructions: [Sol execution brief](./25-sol-execution-brief.md)

This tracker now records implementation and verification after the initial PRD-writing task. Use the current release baseline and latest acceptance log below; historical audit evidence belongs to its original release/date and does not establish current deployment readiness.

Allowed implementation states: NOT_STARTED, IN_PROGRESS, VERIFIED, BLOCKED_EXTERNAL. Record partial verified work inside an entry; do not mark the whole requirement VERIFIED when a required integration is still pending. External blockers need an exact dependency and completed preparation, not a general request for “access.”

## Release baseline

**Authoritative latest checkpoint (2026-09-06T11:12Z):** `reviewed-4f301154fcd9`, source `4f301154fcd9ef538feb46bd1da60a32f7ec5cde48c4db410ff569de04f94032`;346/346 tests and all checks/builds passed with independently matching source. Fly machine22, image `deployment-01M1V6G6GZ2ETKHVMW53VNYZF9`, public health200, startup automation/privacy200, automatic encrypted backup/restore1206ms. Existing Shopify release `pagnetic-v2-04527209-20260906` remains current (later fixes did not change extension source). Actual pixel, authenticated published-theme and current-release re-verification succeed. Synthetic order#1008 and its$10 refund are reconciled: all four inbox rows PROCESSED, TEST_ONLY ledger, zero v2 attribution. Responsive Chrome320/390/768/1440 checks have no horizontal overflow; this is not iOS/performance certification. Approved v2 plan remains VERIFYING with seven operator QA checks explicitly pending and exact migration hold active. Billing/model/shadow/offer remain off. Owner terms date, operational sending connection, remaining QA/recovery/usefulness/distribution evidence are still required; no public-launch claim. Full evidence/next decisions: docs59. All lower release baselines are historical.

**Current live release (2026-09-06T10:46Z):** `reviewed-04527209ccd8`, source `04527209ccd80f350c3fc83f8dc256e83a2469d21730d69605714119e6fe7db5`;343/343 tests plus typecheck/lint/application/Shopify builds passed with matching source identity. Fly machine version20, image `deployment-01M1V51R2B8QRVV4QVEA1F73MT`, health1/1, startup automation/privacy200, automatic encrypted backup/restore1008ms. Shopify pixel/theme release `pagnetic-v2-04527209-20260906` is live. Real storefront events now202; canonical consent and unassigned held Original diagnostic verified in production. Owner-approved plan `cmtpnibba00slq6ld06vi5haa` is now VERIFYING after authenticated published-theme verification; only current-product placement/fallback QA receipts pass. V2 is enabled only under the retained exact test-store migration hold; billing/model/shadow/offer stay off. Synthetic standard checkout confirmed and webhook reconciliation is being verified. Seven scoped operator QA checks and external launch gates remain. See docs59. This supersedes older current-state claims below; it is not public-launch approval.

**Current live release (2026-09-06T10:09Z):** `reviewed-653e304059bd`, source `653e304059bd4df55ebcac3937c6afe36f96f5ffd4d83a61e7c42df28af6f64d`;341/341 tests plus typecheck, lint and builds passed with unchanged source identity, independently checked before deploy. Fly image `deployment-01M1V2ZA2GDEMB2C0AME9KHGPP`, digest `sha256:d4e60ef0f33976eaea82c2f2a2f87b2601de131e16539394a03645be9cd33e9e`, machine version18. Health1/1, startup automation/privacy200 and automatic encrypted backup/restore936ms passed. Same-receipt live retry produced fresh v2 plan `cmtpnibba00slq6ld06vi5haa`, READY_FOR_APPROVAL, approvedAt null, hash `9c3d1d834014e84d4e850dbf1795d75d82fc9afe4f1608a54ac5c32e60ec40db`. Owner approval remains required. V2/billing off and migration hold retained. Customer launch is not approved.

**Newest deployed correction (2026-09-06T09:51Z):** `reviewed-c1ce8652c265`, source `c1ce8652c265d6dde2b88fdbfcf434f934ef26f6f887bedff965922260eb6884`. Full pinned check340/340 plus typecheck/lint/builds passed; main independently verified matching source before deploy. Fly image `deployment-01M1V1YW9XGX140FBMY2JFZNN4`, digest `sha256:7c9efff670df3c9356b503eae0d27a2d7169317749ac9421c7d857ca7a65b0fb`, machine version17; no new migration (27 total). Health, startup automation/privacy200 and automatic encrypted backup/restore1261ms passed. OWNER QA permissions and exact source-invalidated recovery are deployed. Live cutover and synthetic-product reselection succeeded; Original remains held. Subsequent live testing identified a Shopify plaintext sentence-boundary issue now being corrected; no final activation claimed.

**Latest deployed checkpoint (2026-09-06T08:23Z):** `reviewed-3cd01f44ba50`, source SHA256 `3cd01f44ba501a0ab381be8cabe387b1582c9a4a66e8812bc4757266d3d4548d`, 322 source files; Sol's source-bound full check passed338/338 with typecheck, lint and builds. Main confirmed the same source hash before deployment. Fly image `deployment-01M1TWX91QC6905R51HN4MTBZB`, digest `sha256:ccb9fabb50d6d224d2b5687c37361889cca3c359745f21ed12bf013207c6d626`, machine `d8d1497a937658` version16. All27 SQLite migrations applied. Existing legacy plan/approval hashes are unchanged. Health and Fly1/1 checks pass; v2/shadow/model/offer/billing remain false, with cutover allowlist limited to the development store. This is deployment success, not launch approval. See docs55 for live evidence and blockers.

**Current deployed compatibility checkpoint (2026-09-06):** `reviewed-65f2cb62a661`, source SHA256 `65f2cb62a661a53e5aaa5ab6a6e0abcd0a4535c371eaa7d8133cc0b8d5b7ccab`,317 files,26 applied SQLite migrations. Health and legacy approval preservation verified; all v2/billing flags false. Fresh full check passed, PostgreSQL track freshness passed, and automatic off-volume backup/readback/isolated restore passed. The source-bound323-test manifest and table below describe the historical pre-protocol-fix baseline, not certification of subsequent in-flight cutover edits. See the newest implementation log for current blockers.

Current source override (2026-09-05T17:55Z): SQLite history25 migrations, Prisma schema SHA256 `a64d9a707d974d867772b9d512978f850c28783aa19b577699d6281d745ffcfd`; the separate PostgreSQL track is current through69 tables. The corrected source-bound Node24 gate is `docs/release-checks/local-dLdHFh/manifest.json`:323/323 tests plus typecheck, lint, application/Shopify builds, PostgreSQL track/rehearsal and current statistical hashes. No production deployment or migration is implied.

| Field | Recorded value |
| --- | --- |
| Implementation source identity / working-tree manifest | Repository has no commits; all files are user-owned/untracked. The corrected source-bound manifest contains315 files, SHA-256 `875470a389d874efc4d2137c9c652523517106a2e0582f6bea37da4e75fc7275`, Node24.19.0 and completion `2026-09-05T17:55:14.628Z`. It explicitly records deployment/design-partner/public approval false. |
| Schema / migration version | SQLite history contains25 additive migrations through `20260906020000_privacy_delivery_audit`; Prisma schema SHA-256 `a64d9a707d974d867772b9d512978f850c28783aa19b577699d6281d745ffcfd`. Migration24 adds recovery quarantine/replay/release evidence and migration25 adds signed tenant-independent delivery audit. The generated client and migration-to-schema checks pass; the separate PostgreSQL track is current and its actual rehearsal covers69 tables. |
| Backend / extension / deployment version | Package `0.1.0`; Shopify API `2026-07`; theme UID `06f03124-120a-6798-21c0-2bb4e2cf089ab2f75256`; pixel UID `9799ce6c-b318-995d-baa9-c13a2d72dbc4e4ffc9c3`. S02 built legacy theme asset SHA-256 `2430605bba81f195c03fb525124d6fd8eea33db1b138a4ed9df653eb8a1a9c59` and v2 asset SHA-256 `a946bb05fadcba246bfd337bfd2baa6a7057d65b50012710ee1460c31d484f1a`; no deployment performed. |
| Environment and feature flag | `.env.example` and config parser default `PAGNETIC_V2_ENABLED=false`; model, shadow serving, billing and v2 offer publication are independently closed. Intended deployed environment was not changed or verified. |
| Active legacy registrations preserved | VERIFIED locally by populated pre-v2 SQLite upgrade fixture: `pilot-rps-v1`, `REVENUE_PER_SESSION`, registration hash and session assignment remain unchanged with explicit legacy defaults. Actual deployed registrations were not mutated or queried. |
| Baseline checks and audit regressions | Pre-change `pnpm test`: 79/79. Read-only audit probes freshly reproduced zero-order validation, open-enrollment maturity, unused maximum duration, missing-arm validation, duplicate content, incomplete duration and expired-attribution defects. Post-S01 pinned-runtime `pnpm check`: 84/84 plus typecheck, lint, application build and Shopify build passed. |

## Requirements

| ID | Scope | State | Changed paths / evidence / dependencies |
| --- | --- | --- | --- |
| R01 | Diagnosis and useful content | IN_PROGRESS | Public and authenticated v2 paths now produce one source-backed diagnosis, one primary and at most one distinct alternative, or an explicit abstention. Exact campaign text is optional in public preview and required for new UI-created mappings; UTM labels are not treated as ad evidence. Automated 50-case invariants pass; independent human usefulness review remains. |
| R02 | Baseline and feasibility | IN_PROGRESS | V2.3 requires seven full days, mature seven-day outcomes, 2,000 distinct first-eligible visitors, 50 paid purchasers and ≥95% coverage. Checkout-visitor rate contributes the 100-outcome-per-arm floor to both stage forecasts. The plan now bootstraps a fixed 14-day Original/Original cohort, so only complete daily bins reach the authoritative baseline assembler; a qualified immutable snapshot freezes the calculated A/B target instead of defaulting it. Merchant timing now names the actual 56–84-day two-stage window before baseline activation and only the 28–56-day message-test window after baseline completion; it no longer displays the generic 49-day estimate for this plan. The 2,100-visitor/202-reconciliation assembler and full plan transition pass locally. Ordinary page views remain traffic-only; real-store source/traffic evidence is not claimed. |
| R03 | Review and enablement | IN_PROGRESS | Ordinary merchant Messages and Preview screens show the actual synced product image/description beside the exact source-backed proposal, support versioned edits/revalidation, evidence trace and explicit owner approval. The Overview uses the selected product handle/template suffix for the theme deep link and gives two concise save/verify steps. Product-specific runtime acknowledgment, duplicate/wrong-context recovery and real browser evidence remain S10. |
| R04 | Deployment authority | IN_PROGRESS | Immutable v2 deployment revisions, one product pointer, stale-action rejection, runtime authority lease and pause fallback are locally tested. Actual Shopify app-proxy transport and visible-page pause rehearsal remain S10. |
| R05 | Cohort and lifecycle | IN_PROGRESS | Fixed seven-day visitor assignment, no expiry renewal, health-informed target/minimum-duration or hard-deadline close, immutable cohort dates, pause semantics and one-time mature finalization are locally tested. Early close requires at least 100 distinct observable non-test checkout outcomes in each arm under the experiment lock. Tenant-scoped durable jobs and the approved plan now execute fixed Original baseline → mature qualification → frozen Original/Universal test → exact pinned result. Serving activation, pointer, evaluation consumption, plan phase, outbox and receipt share one transaction. Intended scheduler operation remains unverified. |
| R06 | Financial ledger and attribution | IN_PROGRESS | Durable sanitized inbox, complete-read GraphQL adapter, exact-minor-string canonical ledger, watermark precedence, convergent refund revisions and signed product-line attribution are locally tested. Actual Shopify API validation, tax-inclusive source convention and production worker operation remain unverified. |
| R07 | Analysis and report | IN_PROGRESS | Assigned-visitor Welch analysis includes zero outcomes, enforces result precedence and uses immutable financial as-of facts with an exact final snapshot pointer. Report generation derives persisted health internally; caller READY cannot waive it. Duplicate/missing-link/unknown-context/render fault tests pass, frozen reports preserve their health assessment and the plan exposes only the exact final pointer. Independent SciPy calibration remains green. Late-source operational health semantics and intended-store evidence remain. |
| R08 | Merchant status and actions | IN_PROGRESS | `MerchantStatusV2` derives severity, serving, owner, action and blockers together; contradictory “No action needed” copy is removed. The default navigation is Overview/Messages/Results/Settings, with pause, verified subscription wording, evidence-backed surface states, incidents and a PII-excluding merchant export; operator workspaces are under Advanced. A visible-only bounded Overview refresh now advances the asynchronous preparing state without interrupting focused edits. Actual-component local Chrome/AX/keyboard/zoom/width evidence is recorded in `docs/40-local-ui-accessibility-qa.md`; authenticated embedded/mobile coverage remains AT28/S10. |
| R09 | Keep/revise/stop | IN_PROGRESS | Authenticated OWNER/OPERATOR result actions now verify the exact final snapshot, registered approved content, current subscription authority and product deployment revision. Keep installs a nonexperimental approved pointer; revise creates one separate source-backed draft; stop advances to an Original/STOPPED pointer while financial reconciliation remains independent. Local idempotency and frozen-report preservation pass; live serving remains S10. |
| R10 | Billing lifecycle | IN_PROGRESS | Authoritative subscription verification is tenant-bound, watermark-aware and defaults ambiguous/conflicting states to FROZEN. Existing $49 founding terms remain nonpublishable; the drafted $99 offer requires two explicit commercial flags. Atomic serving authorization persistently consumes one measurement check, one message test and at most one eligible negative/inconclusive revision across new IDs/reinstall; paid authority bypasses the free ledger. A bounded official Partner API active-subscription adapter and Admin shop-ID verifier are locally tested and never create a charge. Real Partner credentials, pricing-page publication/callback behavior and no-charge dev-store observation remain external. |
| R11 | Operational reliability | IN_PROGRESS | Tenant-scoped jobs/outbox have bounded leases/retries/dead letters; financial inbox work can reclaim expired processing, renew before reconciliation, reject stale workers and retain incomplete canonical reads as scheduled retries. Durable v2 close/finalize jobs use one stable root chain, actual health and bounded successor keys. First-view catalog sync, opportunity preparation and Web Pixel setup now execute in a tenant-scoped durable offline-capability worker rather than the page loader; concurrent refresh dedupe, retry, terminal non-resurrection and explicit retry pass locally. Pixel delivery has finite memory queues, per-attempt deadlines and consent-epoch abort/drop behavior; uncancellable storage mutations retain one settled serialization chain across revoke/regrant. Incident delivery requires HTTP 2xx and encrypted backup/restore evidence is locally tested. A four-tenant local mixed-service rehearsal at twice its declared synthetic fixture rate has zero errors and records latency/fallback/queue/RSS/fairness; real scheduler, alert endpoint, off-volume backup, DB waits and production-shaped load remain. |
| R12 | Privacy and isolation | IN_PROGRESS | The ordinary code-controlled path passes locally: exact encrypted intake, leased collection, transactional order/identity suppression, durable lookup/storage key history, exact graph erasure, analysis invalidation, signed restore-journal replay, Shopify-account-owner-verified post-reinstall access and explicit all-part delivery confirmation. Recovery authenticates a completed delivery without recollecting or extending it, including after artifact purge; missing/tampered audit fails promptly. Redaction remains backup-review and delivery remains legal-review-required. Over-limit/lost-scope review, real retention/off-volume recovery, actual Shopify-owner delivery and live consent/isolation evidence remain external. See docs44–48. |
| R13 | Real browser/commerce evidence | IN_PROGRESS | Product/variant-owned form bridge and pixel reference scoping are implemented and unit-tested. Theme/checkout/accelerated-commerce rehearsal is not yet performed. |
| R14 | Release and learning instrumentation | IN_PROGRESS | Protocol/metric constants, default-closed rollout flags, reproducible migration evidence and exact S02 asset hashes/sizes are recorded. The legacy/v2 runtime assets are 9,992/9,999 bytes after the consent-race corrections. |

## Work packages

| Execution order | ID | State | Changed paths / checks / release |
| --- | --- | --- | --- |
| 1 | S01 | VERIFIED | Local S01 slice: schema/migration, flags, job/outbox foundation and truthful status. `pnpm check` passed on Node 24.19.0; AT21 and AT29 local regressions pass. No external/deployed verification implied. |
| 2 | S02 | IN_PROGRESS | Code-controlled implementation is locally verified behind the default-off flag: consent identity/baseline, assignment-arm telemetry, frozen-policy assignment, signed bridge for both arms, deployment CAS/authority lease, immediate local lease expiry, abort/epoch-safe consent reversal, bounded Pixel delivery/storage queues, settled cross-epoch storage serialization, pause/expiry/closed-enrollment behavior and split theme runtime. Pixel VM tests pass12/12 and Shopify builds the extension at7.5KB original/~3.0KB compressed. Real app-proxy/consent/theme/checkout evidence remains S10, so the integration package is not marked VERIFIED. |
| 3 | S03 | IN_PROGRESS | Code-controlled financial path is locally verified: authenticated routes durably record a PII-minimized inbox before acknowledgment; an idempotent job fetches the canonical parent; the pinned 2026-07 adapter paginates lines and refund children, verifies exact transaction coverage and rejects changing watermarks; exact string-minor ledgers converge across replay/correction; signed product-line references drive attribution. Focused tests 14/14 and full check 133/133 pass. Actual Shopify queries/webhooks, tax-inclusive refund convention and production worker/scheduler remain external/integration evidence. |
| 4 | S04 | IN_PROGRESS | Code-controlled lifecycle/analysis includes tenant/product-isolated mature baseline qualification, explicit target/deadline close, assigned-visitor Welch analysis, persisted health precedence, immutable as-of finance and exact final report pointers. Independent multi-regime calibration and plan integration pass locally; production scheduling and intended-store evidence remain. |
| 5 | S05 | IN_PROGRESS | Source-backed diagnosis/composition, abstention, exact campaign evidence, tenant-scoped persistence and public/authenticated input paths are locally verified. Focused tests 15/15 and full `pnpm check` 160/160 pass. Human 50-item usefulness review and S06 ordinary-merchant review flow remain. |
| 6 | S06 | IN_PROGRESS | Locally compiled merchant flow now covers actual source/proposal review, versioned edit/revalidation, approval separation, selected-template enablement link, measurement/progress, pause, exact frozen-result pointer, post-finalization financial-review notice, subscription/surface/incident settings and safe JSON export. Targeted S06/presentation/readiness tests pass 25/25; integrated pinned `pnpm check` passes 175/175 plus typecheck, ESLint, production build and Shopify build. Product-specific published-runtime acknowledgment, Shop Pay applicability and AT28 browser/a11y evidence remain S10; keep/revise/stop receipts remain S07. |
| 7 | S08 | IN_PROGRESS | Queue/outbox repair, HTTP-verified incident delivery, encrypted remote-readback/isolated-restore, webhook-inbox crash leases, scoped workers, exact erasure and restore quarantine are locally tested. Signed replay evidence plus one atomic release audit are required before startup/SQLite→PostgreSQL transfer; unsigned RELEASED fails closed. Historical recovery chains authenticate through bounded retained backup keys, while restore preserves the exact verified manifest beside the new database with collision rollback. Real storage/scheduler/monitoring/retention and full intended-environment recovery remain. |
| 8 | S07 | IN_PROGRESS | Result actions, subscription authority and persistent evaluation consumption are locally implemented: exact final/registration/approval/entitlement guards, atomic keep/stop pointer revisions, one idempotent revise draft, explicit provider-state mapping and default-closed offer publication. Free serving claims exactly one A/A and message-test slot plus one eligible revision using immutable unique ledger rows. The official Partner API active-subscription adapter, Admin shop-ID verification and merchant refresh action pass locally without creating charges; real pricing-page/no-charge dev flow and provider credentials remain. |
| 9 | S09 | IN_PROGRESS | Separate PostgreSQL migration track and actual local SQLite→PostgreSQL parity/dump-restore rehearsal;69-table hashes, exact money/history, two-worker exclusive leases, lifecycle/financial/deployment/privacy/report interleavings, held-transfer refusal and authenticated released-transfer acceptance are verified on fixtures. The small SQLite mixed-service rehearsal is not provider capacity evidence. Provider packaging, production-shaped load/DB waits, managed recovery and operational cutover remain. |
| 10 | S10 | IN_PROGRESS | A fail-closed localhost-only fixture renders the actual Overview/Messages/Results/Settings views; Chrome152 AX, keyboard,200%-zoom and exact320/375/768/1024/1440 widths pass. Verified-owner privacy access/confirmation and reinstall-generation guards pass locally. This is not Shopify integration evidence; embedded App Bridge, real token exchange/download, published storefront, privacy API, app proxy, checkout, supported mobile browsers and live pause remain. |

## Acceptance evidence

The exact scenarios are defined in PRD section15. Evidence must identify environment, timestamp, fixture/browser path, expected and actual outcome, and release identity. N/A requires a documented applicability rule and evidence; it is not equivalent to PASS.

| Test | State | Evidence |
| --- | --- | --- |
| AT01 | IN_PROGRESS | Local install service creates a product-scoped revision/pointer/outbox atomically with no storefront IDs required; fresh Shopify save and acknowledgment remain unverified. |
| AT02 | IN_PROGRESS | Local approved-plan fixture executes fixed Original/Original baseline, consumes the measurement slot, reads the exact mature result/qualification, atomically advances the same product pointer to the frozen Original/Universal registration and exposes only the exact final result. The Liquid block contract remains unchanged; actual same-block Shopify observation remains S10. |
| AT03 | IN_PROGRESS | Exact local retry is idempotent and stale revision is rejected. Actual two-client PostgreSQL deployment rehearsal now covers six conflicting revisions and simultaneous identical retry, with seven total commits/receipts/outbox records and one pointer; it exposed and corrected the empty-upsert locking defect. Full approved experiment/plan transition contention and real Shopify observation remain separate integration scope; see docs42. |
| AT04 | IN_PROGRESS | Local service proves paused new requests are Original; client lease rules revoke changed/paused treatment while retaining a valid measured control. Visible browser ≤30-second observation remains S10. |
| AT05 | IN_PROGRESS | Local zero-order/degenerate A/A analysis cannot pass measurement checks or launch A/B. Persisted health, 100 observable non-test checkout outcomes per arm, immutable mature baseline and plan-transition guards are integrated locally. Intended-store evidence remains. |
| AT06 | IN_PROGRESS | Open enrollment always returns COLLECTING and a closed cohort before its financial date returns MATURING; neither produces a final claim. Durable jobs invoke close/finalize with actual persisted health; intended external scheduler operation remains unverified. |
| AT07 | IN_PROGRESS | Local early-close service requires target, minimum duration and at least 100 persisted distinct observable non-test checkout outcomes in each arm. The hard-deadline worker still freezes close at day42 if under target, enqueues financial maturity and persists one INSUFFICIENT_EVIDENCE result without inventing a winner. Runtime deadline fallback is covered in S02; combined browser/intended-environment evidence remains. |
| AT08 | IN_PROGRESS | Local database fixture counts a purchase just inside the fixed expiry even when observed later and quarantines a purchase exactly at expiry. Real Shopify purchase-time evidence remains S10. |
| AT09 | IN_PROGRESS | Duplicate webhook identity is a no-op; refund-first inbox processing fetches the parent; canonical watermark replay and a later refund correction converge without double deduction. Real reordered webhook delivery and bounded worker recovery remain S08/S10. |
| AT10 | IN_PROGRESS | Exact fixtures cover paid, authorization-only, unpaid/partial, cancelled-paid pending reconciliation, settled partial refund and QA/test orders without live-revenue inflation. Controlled Shopify payment/refund fixtures remain unverified. |
| AT11 | IN_PROGRESS | Local fixtures isolate focal merchandise in mixed carts and exclude gift-card product lines; signed attribution is per order line/product. Actual native/accelerated checkout retention remains S10. |
| AT12 | IN_PROGRESS | Tampered, cross-tenant and exact-expiry signed references create no attribution, quarantine the order and write an audit record in isolated database fixtures. Live webhook tenant replay remains S10. |
| AT13 | IN_PROGRESS | SQLite fixture counts distinct consented product visitors/sessions before any Pagnetic decision and excludes analytics-only consent; live pixel baseline continuity remains S10. |
| AT14 | IN_PROGRESS | Unit/VM fixtures cover missing/denied consent, fixed non-sliding token expiry, late eligibility path, revoke cleanup, mixed-product form isolation, lease invalidation, active-fetch abort, old-epoch queue drop, ordered timeout recovery, finite delivery/storage bursts and deferred revoke/cleanup mutations that settle only after regrant. Supported-browser privacy event evidence remains S10. |
| AT15 | IN_PROGRESS | Local fixtures cover one missing arm, all-zero/degenerate variance and zero-order outcomes with explicit INSUFFICIENT_STATISTICAL_INFORMATION; Student-t numeric references pass. Independent 24,000-null and 12,000-positive-effect simulations pass the frozen calibration guards. Intended-environment execution remains. |
| AT16 | IN_PROGRESS | Qualification refuses a five-day/two-visitor baseline and never substitutes 1,000 visitors or a 28-day assumption. Checkout-rate forecasts can extend or reject the run; the fixed 14-day baseline bootstrap feeds the mature assembler, and only its frozen calculated target starts A/B. Real traffic timing remains unverified. |
| AT17 | IN_PROGRESS | Public preview no longer emits multiple renamed angles. Exact repeated source bodies abstain, and an identical candidate cannot be stored under a second angle; focused source/draft regressions pass. Human visual/content review remains. |
| AT18 | IN_PROGRESS | Local fixtures reject unsupported campaign claims, unsupported locale/category, contradictions, high-risk claims, instruction-like source/ad content and oversized ad input. Generated claims are exact source spans and tenant-scoped persistence is tested; independent human evaluation and live UI evidence remain. |
| AT19 | IN_PROGRESS | Unit regression proves the selected product handle and actual template suffix are encoded into the official theme-editor deep link; merchant enablement copy gives preview/save then verify. Duplicate-block, inactive Save, wrong published context and authenticated product-runtime acknowledgment still require browser/integration evidence; activation cannot be called fully verified from the current local test. |
| AT20 | IN_PROGRESS | Settings renders PASS/FAIL/PENDING/NOT_APPLICABLE evidence per surface, marks expired records explicitly and does not infer absent/unknown capabilities as supported. Controlled Shop Pay enabled/N/A/pending evidence remains S10. |
| AT21 | VERIFIED | Local pure presentation regression and compiled route at 2026-09-05T08:20Z: blocking VERIFYING owns repair, names support CTA, serves Original and contains no “No action needed” claim. Browser evidence remains part of S10, not this local result. |
| AT22 | IN_PROGRESS | Pure analysis covers positive, negative, inconclusive, invalid, insufficient, interrupted and A/A-check states. The merchant Results route selects only the exact final snapshot pointer, requires payload/snapshot state agreement plus exact minor units before a monetary headline, shows the registered interval/scope and suppresses money otherwise. Keep/revise/stop actions remain S07 and browser state-matrix evidence remains S10. |
| AT23 | IN_PROGRESS | Isolated database fixture executes keep, idempotent replay, separate source-backed revise, stop and stop replay; pointer revisions advance, serving returns to Original/STOPPED, exact action receipts are retained and the final report/pointer remain unchanged. Real approved storefront observation and the next draft approval are S10. |
| AT24 | IN_PROGRESS | Provider fixtures cover active, no active contract, declined, frozen fallback, paid-through cancellation, expired cancellation, tenant spoofing, exact replay, older update and same-watermark conflict. The official Partner API adapter is bounded and read-only; a unique persistent ledger prevents new experiment IDs or reconciliation resets from granting another measurement/message/revision allowance. Shopify-hosted pricing/callback and actual reinstall remain. |
| AT25 | IN_PROGRESS | Isolated SQLite, VM and stub HTTP regressions cover expired job/outbox/inbox/privacy leases, reclaimed-worker rejection, bounded crash retries, incomplete canonical follow-up, stable lifecycle/onboarding chains, dead-letter non-resurrection, Pixel bounds, HTTP delivery, stale backup proof, exact privacy replay, signed restore quarantine/release, concurrent single-winner release and transaction rollback. Actual external delivery, remote-side-effect crash recovery and intended-environment backup freshness remain. |
| AT26 | IN_PROGRESS | Real local PostgreSQL16.15 transfer and dump/restore preserve populated legacy/v2 fixture rows, money, IDs, approval/report/privacy hashes and FKs across69 tables; provider migration history/diff, held-transfer refusal and authenticated released-transfer pass. Representative production data, managed PITR and full application RPO/RTO remain unverified. |
| AT27 | IN_PROGRESS | Reproducible isolated SQLite rehearsal schedules 16 real v2 decisions, 4 durable webhook accepts and 2 report reads per second for five seconds—twice its explicitly declared synthetic fixture forecast. It completed 110 operations/zero errors; decision p50/p95/p99 was10.066/16.774/26.566ms, every tenant completed20/20 decisions, no unexpected fallback occurred, and four 1,600ms mobile deliveries all returned Original at the 1,500ms deadline. `docs/audit-2026-09-05/mixed-load-v2.json` binds source hashes and records queue age/RSS/limitations. DB waits, production-shaped volume, actual forecast, PostgreSQL/Fly/Shopify and device Web Vitals remain unverified. |
| AT28 | IN_PROGRESS | Actual route views render through a PII-free localhost-only harness. Chrome 152 at 200% and exact 320/375/768/1024/1440 CSS widths showed no horizontal clipping/overlap in Overview/Messages/Results/Settings; every cell measured maximum scroll width equal to inner width. AX exposed semantic headings, labeled native fields/controls, frozen financial evidence, entitlement-closed actions and incident/surface state; keyboard order was logical with no trap. VoiceOver/mobile browsers, authenticated background-edit preservation and embedded Shopify context remain. |
| AT29 | VERIFIED | Populated pre-v2 SQLite database upgraded through additive migration; old protocol, primary metric, registration hash and session assignment preserved. Full legacy suite also passed in the 84-test check. Mixed deployed client remains S10 evidence. |
| AT30 | IN_PROGRESS | Local stop leaves financial/privacy reconciliation claimable; shop redaction cascades financial revisions/links plus evaluation rows, and uninstall disables serving while retaining required ledgers. Exact customer graph erasure, keyed replay denial, post-uninstall data-copy collection and Shopify-verified post-reinstall retrieval/delivery confirmation pass locally. Authoritative live Shopify subscription/privacy events, backup deletion and deletion/reconciliation ordering remain unverified. |

## External dependencies

Current provider audit: docs48 records the live Fly release/configuration check. A subsequent read-only `flyctl storage list --org personal` succeeded with no bucket rows; no Fly-integrated Tigris storage exists in the accessible organization. Independently owned S3-compatible storage remains unknown, not proven absent. Docs16 now records the exact remaining storage, key-escrow and incident-destination inputs without re-requesting the already confirmed identity, Fly billing or deployment authorization. PRD section18 defines the unchanged safe defaults.

For each actual blocker record: capability; exact missing value/approval; evidence of the blocker; completed safe preparation; one owner action; independent work still available. Keep secrets out of this file.

## Completion gates

| Gate | Status | Required evidence |
| --- | --- | --- |
| Engineering complete | VERIFIED_LOCAL | All currently identified safe code-controlled requirements pass the source-bound Node24 gate; human/provider/browser evidence remains separate. |
| Design-partner deployable | BLOCKED_EXTERNAL | Requires verified intended environment, supported Shopify commerce, recovery/alerting, qualification and external authority. |
| Public acquisition ready | BLOCKED_EXTERNAL | Requires design-partner evidence, assisted-cohort activation, decisions, paid continuation, legal/distribution and support evidence. |

## Implementation log

### 2026-09-06 — Explicit owner approval recorded for synthetic test plan

User explicitly said "Test plan approved" after reviewing the exact synthetic travel-pouch proposal. Main checked the unchanged visible content, selected the review checkbox and submitted Approve and prepare test through the authenticated Shopify UI. Browser confirmed approval; readonly production DB confirms plan cmtpnibba00slq6ld06vi5haa is WAITING_FOR_THEME with approvedAt1788690016791, unchanged planHash9c3d1d834014e84d4e850dbf1795d75d82fc9afe4f1608a54ac5c32e60ec40db and selectedreceipt cmtpmvlu500f3q6lbj336xp2g. V2 and billing remain false and the exact migration hold remains active. This approves only the displayed test content/plan, not missing QA evidence, legal terms, billing or public launch. Next: published theme verification, runtime acknowledgement under the retained hold and actual scoped QA.

### 2026-09-06 — Shopify plaintext correction deployed; fresh plan awaits owner

Source-bound341-test package deployed as reviewed-653e304059bd. Backup before deploy: pagnetic-25265b17-b7dd-4432-91b9-2b99d211da8b.sqlite.enc, restore990ms; standard retention removed one obsolete verified local cache pair, remote objects retained. Automatic postdeploy backup pagnetic-6b9e773d-9c6c-43c7-ad66-77d04d379976.sqlite.enc restored in936ms at10:09:38Z. Startup automation/privacy both200. Authenticated browser same-product retry then bounded automation200 created READY_FOR_APPROVAL v2 plan cmtpnibba00slq6ld06vi5haa under unchanged selectedreceipt cmtpmvlu500f3q6lbj336xp2g. Browser and readonly DB agree approvedAt is null. Exact Universal headline: "One pouch is included in each test order." Benefits retain synthetic disclosure, no-real-item disclosure, zip closure and two internal pockets. No checkbox or Approve button was pressed; user expressly retains approval responsibility.

Remaining UI defect observed: old unsupported-content and retry-limit notices are still OPEN/displayed beside the successfully prepared opportunity, causing a stale unsafe/needs-attention headline. This is not a new preparation failure; reconcile those exact successful-preparation notices in the next bounded correction while retaining unrelated safety notices and audit history. Do not call merchant UX acceptance complete. Remaining external gates: owner content/QA review, approved terms date, authenticated mail delivery for selected recipient bilgi@flapp.ist, full incident-recovery review, human content usefulness evaluation, and live post-approval QA/activation. No final-launch claim.

### 2026-09-06 — Owner QA and invalidated-source recovery deployed; selected fixture held

User explicitly retained QA approval as the existing OWNER; no role was created or reassigned. Source-bound340-test release deployed as reviewed-c1ce8652c265. Backup before deploy: pagnetic-2f629fe2-0ae3-4ec1-bd14-44df8ee442bc.sqlite.enc, restore1121ms. After deploy: pagnetic-a9d4d447-19af-406d-bf98-8f5785598415.sqlite.enc, restore1261ms at09:51:49Z. Legacy approval/hash unchanged, existing source invalidation preserved. Authenticated browser created root receipt cmtpmv67l007kq6lbxatynqvv then synthetic selection receipt cmtpmvlu500f3q6lbj336xp2g; exact migration hold remains active, v2 and billing false. Fresh manual bounded automation returned200. Synthetic diagnosis correctly abstained given current parser input, but investigation found Shopify concatenates HTML paragraph text without spaces (e.g. fulfilled.A zip ... together.Two internal ...). This produces fewer than three extracted spans despite distinct source facts. Sol is fixing safe segmentation and receipt-bound retry; no fabricated facts, test PASS flags or owner approvals were inserted.

### 2026-09-06 — Browser recovered; Shopify upstream errors hold continuation

Browser access recovered and the deployed Overview exposed the explicit cutover action. Its single submission correctly returned V2_CUTOVER_SOURCE_CHANGED. Refresh and one recovery reload both showed Shopify's own500 error page. Read-only AutomationRun inspection attributes08:48/08:53 scheduled failures to Shopify GraphQL500, not SQLite contention; the08:58 run completed. Retained paired endpoint logs show automation/privacy200 at08:43 and08:58, with health200. No fresh approval, QA receipt or serving flag was bypassed. Continue with current source review after Shopify recovers; owner/operator/legal/alert gates remain as documented in docs55.

### 2026-09-06 — Completed cutover package deployed; activation held

Deployed the reviewed338-test package and serialized maintenance fix following a fresh encrypted pre-deployment backup/restore. Startup automation and privacy both returned200 sequentially at08:23:48Z. Automatic post-deployment backup/readback/isolated restore passed at08:23:50Z. Two subsequent natural scheduled cycles remain to be observed; startup alone does not close that acceptance gate. Public health/privacy/terms/support routes returned200. Production readiness still fails exactly alert delivery and public identity configuration; its backup check passes but does not certify the independent incident-recovery release drill. Shopify browser access is blocked by the locked Mac. Live role inventory has one active OWNER and no OPERATOR; no role was self-granted. No cutover receipt, new owner approval, treatment, billing activation or customer launch was fabricated. Exact handoff: docs55.

### 2026-09-06 — Isolated synthetic product prepared

- Created a clearly disclosed synthetic travel-pouch fixture as a DRAFT in the authorized development store, product10345426977074. Exact source facts and simulation boundaries are in docs54; Shopify confirmed creation/save. No public availability, real inventory/fulfillment promise, existing product mutation, approval or experiment change.
- This prepares the supported product-reselection/live-test path without inventing facts about the current snowboard. Publication, sync, selection, source validation and fresh approval remain unperformed. Sol continues the bounded evidence-intake/reselection corrections.

### 2026-09-06 — Cutover package full check green; bounded final-path gaps

- Sol completed core selected-store receipt, legacy retirement, source refresh, fresh approval, theme/runtime observation and exact-hold release. Dedicated regressions30/30; integrated pinned Node24.19 check336/336 plus typecheck/lint/builds; PostgreSQL rehearsal69 tables/15 rows/12 claims. Main's sequential-maintenance correction is included. Package evidence: docs52. No deployment of this source yet.
- Main's final executable-path review found that production writes only placement/original_fallback QaEvidence while activation requires nine keys; the other seven have no supported evidence intake. Successful fixtures therefore do not prove an executable operator workflow. Also, runtime returns V2_DISABLED before KILL_SWITCH_ACTIVE, contradicting docs52's flag/acknowledgment order. Receipt scope requires the legacy product, and the abstention path does not yet provide a supported fresh test-product selection/retry path.
- Sol is resolving only these bounded workflow gaps, preserving real evidence requirements and historical authority. No manual database evidence insertion, invented QA pass or feature activation is permitted as a workaround. Existing green results are retained as local evidence, not complete S10 certification.

### 2026-09-06 — V2 financial adapter against actual Shopify refund

- Used deployed v2 canonical provider adapter and normalizer (API2026-07/policyv2.2) read-only against already-refunded test order#1007. Complete lines/transactions/refunds/refundChildren, no GraphQL errors, one line/one refund/three transactions, original obligation69995USD minor units, no unresolved reasons.
- Actual order correctly remains TEST_ONLY in payment/reconciliation; efficacy focal-net accessor returnsnull. No v2 ledger or attribution was written, no order changed and no synthetic customer identifiers exported. This narrows R06/AT10's provider-validation gap for the full-refund test shape, not the entire lifecycle. Exact limits in docs27; partial refund/mixed cart/inbox/real efficacy remain unverified.

### 2026-09-06 — Recurring maintenance contention corrected locally

- Next natural recurrence07:27:43Z disproves the startup-only hypothesis: automation207 and privacy503 both after5s, while public health remains200. The earlier manual sequential run was200/200 in483ms/7ms.
- Main replaced independent automation/privacy timers with one sequential bounded cycle, preserving separate endpoints/auth,90s request bounds,207 failure semantics and restart on ambiguous network failure. A completed failure still permits the other maintenance endpoint; unknown completion does not. Backup cadence unchanged; no general SQLite capacity claim.
- Focused real-HTTP/process supervisor suite10/10 and ESLint pass. Details and required two-natural-cycle deployment acceptance: docs53. This change is local only and will join Sol's cutover package for one coherent full check/deployment. No second deployment of in-flight source occurred.

### 2026-09-06 — Post-restart operational follow-up

- Supervisor logs confirm a repeat of the earlier startup-only contention symptom: at07:22:38Z automationRun.create timed out after5s, automation returned207 and privacy returned200 after roughly5s. Backup succeeded concurrently. Health remained200 after startup. This is not a clean operational pass or proven root cause.
- A bounded sequential authenticated retry after startup returned200 for both automation and privacy; no secret or response payload was exported. Sol has the exact scheduler contention observation queued after the current cutover package. No failure was hidden, no retry loop added, and no database timeout relaxed.

### 2026-09-06T07:23Z — Compatible deployment and migration verified; cutover implementation gap

- Fly image `deployment-01M1TSC2XS2SC02491GCXA3VX3`, digest `sha256:ad88ea21b08296fa236b1232d89967f709b1da1f0139e6f421e8a695a6e146b9`, is running. After normal image replacement/startup, public health200 and Fly1/1 checks pass. Source identity remains the317-file hash below, unchanged after build.
- Live migration count26; legacy plan remains VERIFYING and now has `orchestrationProtocolVersion=autopilot-plan-v1`. Both recorded plan and approval hashes match exactly. No legacy approval reuse, content rewrite or v2 activation occurred.
- New supervisor backup `pagnetic-dc140b07-bfea-40b7-a18d-433ac6b831fa.sqlite.enc` created07:22:34Z under `reviewed-65f2cb62a661`; remote readback and isolated restore1006ms, integrity/FKs OK, encrypted empty privacy journal. Full incident reopening remains separate.
- Fresh intended-environment production checker confirms exactly two configuration failures: external alert delivery destination and public identity (approved terms effective date remains missing). Backup evidence passes. This checker is not a substitute for outstanding S10 journey evidence.
- Sol's production-path cutover inspection found concrete remaining engineering gaps: no selected-store migration receipt/action to retire the legacy plan; global-only preparation selection; stale catalog reuse; no production QaEvidence writer; theme activation does not stamp authenticated verifier/time; and no governed clearance of preparation pause. Existing direct-fixture tests do not prove these paths. Sol is implementing the bounded authenticated cutover/activation path and regressions; no manual DB bypass, fake QA evidence or global activation is authorized by this checkpoint. Goal remains active and launch unverified.

### 2026-09-06 — Protocol boundary checked; compatible deployment running

- Sol completed the bounded persisted-plan protocol repair (docs51). Fresh full `pnpm check` completed with exit0, including tests, typecheck, lint and both builds. Current source identity:317 files, SHA256 `65f2cb62a661a53e5aaa5ab6a6e0abcd0a4535c371eaa7d8133cc0b8d5b7ccab`. The exact total test count was truncated in recovered terminal output; no new count is inferred. Fresh PostgreSQL track `--check` also passes; this does not claim a new full PostgreSQL rehearsal.
- Live preflight still has25 migrations and the original VERIFYING plan `cmtnb5w6j003fq6kvny97khn7`. Plan hash `0e454982fc549815a977a7748fbf945e5424790a65a6a00b7196945770187911`; approval JSON SHA256 `15f8658663588d2263b4b6f08042b5a0b72987946018770eaa26bffb5002b1ef`. Only hashes, not approval contents, were exported for preservation verification.
- Fresh predeployment encrypted backup `pagnetic-2b939806-1b4e-49c4-9d6d-410d0792b2aa.sqlite.enc`, created07:14:46Z, records remote readback, integrity/FK success and985ms restore. Empty encrypted privacy journal accompanies it. This is database recovery evidence, not full application reopening approval.
- Authorized compatible Fly deployment is running under release identity `reviewed-65f2cb62a661`; v2/shadow/model/new-offer/billing explicitly false. Migration26 and post-restart preservation/health are not yet verified. No fresh v2 plan has been created and no old approval repurposed. Next: verify deployment, then the explicit fresh-plan test-store cutover.

### 2026-09-06 — Shop Pay applicability and confirmed activation defect

- Read-only Admin payment settings: Test payment gateway Active, Shopify Payments Complete setup, additional providers empty. Together with real checkout evidence, Shop Pay is NOT_APPLICABLE only for the current test-gateway configuration; not a passed Shop Pay integration. No payment settings changed. Evidence: docs50.
- Sol confirmed the legacy dispatcher defect: global flag alone can send the existing v1 approved VERIFYING plan to v2 activation. Bounded persisted-protocol/approval-dispatch regression fix is in progress. Backend v2 remains false; no live legacy approval is repurposed. This is an engineering blocker, not a request for another owner approval.

### 2026-09-06 — Saved Autopilot block and activation migration preflight

- Refreshed stale Shopify theme editor schema, changed only existing Adaptive Panel runtime policy from legacy experiment to Pagnetic Autopilot, saved and verified Save disabled. Live product DOM confirms mode=autopilot, block-version=adaptive-panel-v2, one hidden panel; Original stays visible. Historical experiment ID retained unchanged.
- Shopify native mobile preview creates an actual375px-wide iframe; visual inspection confirms readable wrapping of product title and variant controls. This is theme-editor Original layout evidence only, not active-treatment/mobile-checkout/performance certification.
- Live DB preflight: one merchant, zero active deployments/versions; the one approved plan remains VERIFYING with pilot-safety-v1. Dispatcher currently branches solely on global V2_ENABLED. Sol assigned a bounded legacy-approval-routing regression/fix investigation before backend flag activation; v2 stays disabled until verified. No historical plan approval was changed or reused.

### 2026-09-06 — Reviewed Shopify extension released

- Created and released Shopify version `pagnetic-v2-reviewed-20260906` (ID1117556736001); CLI validation/build/release succeeded. This is the app configuration/extension release, not App Store publication or new commercial approval.
- Actual test-store product DOM now confirms this CDN version, v2 script and one v2-versioned block; Original remains intact. Existing saved block mode remains `experiment`, so v2 Autopilot activation still requires the governed test-store setup. Backend v2, billing, shadow, model and new-offer flags unchanged/disabled.
- Old-extension gap from docs50 resolved; active v2 journey, alerts, legal date, full recovery release and other acceptance remain open. No launch completion claimed.

### 2026-09-06 — Live end-to-end test, partial pass

- Fresh323/323 tests plus typecheck/lint/application/Shopify builds passed; source manifest unchanged. Overnight six-hour backup and repeated five-minute scheduler requests verified.
- Actual native test-store purchase #1007 paid through Bogus Gateway and fully refunded/restocked; webhook204s and legacy StoreOrder net0/test=true verified. Variant keyboard selection and Buy it now handoff preserve Dawn variant. Embedded Overview/Results truthful, no uplift fabricated.
- Tigris restore into a new isolated Fly-volume DB, independent empty receipt replay and startup quarantine enforcement passed. Full audited application reopening remains pending, without invented review references.
- Complete v2 acceptance remains blocked by old Shopify extension/current disabled v2 configuration, external alerts and approved terms date. Mobile override did not take effect; not passed. Shop Pay/active-v2/mixed-cart/consent/performance remain open. Exact coverage and limitations: docs50. No blanket launch-ready claim.

### 2026-09-05T20:34Z — Owner-confirmed recovery and runtime activation

- Owner confirmed independent protected-note retrieval. Reviewed source manifest reverified unchanged, remote application build passed, and Fly machine version14 deployed (`deployment-01M1SMB1C9M9M9BE491RJZ9GKN`). All25 migrations applied; one merchant preserved; all14 secrets deployed. Billing and v2/shadow/model/new offers remain disabled.
- Fly health1/1 passing and public health200. Supervisor's first automatic six-hour-schedule backup uploaded/read back/restored successfully, including the empty encrypted privacy journal. Exact artifact and image identities are in docs49.
- Initial automation207/SQLite timeout during startup; bounded post-startup retry200. Privacy200. Longer-run recurrence and production load are not yet certified. No full application recovery, Shopify extension publication, real-merchant acceptance or public launch approval is implied.

### 2026-09-05T19:35Z — Real Tigris backup and staged configuration

- Owner-created Frankfurt/Standard private storage is connected through the CLI. Dedicated policy and real SDK checks verify encrypted read/write, prefix listing, conditional overwrite protection, denied deletion/out-of-prefix writes/organization listing/anonymous access and preserved ciphertext. Documented provider limitation: bucket-level listing cannot be conditioned to a prefix; use this dedicated bucket only. Details in docs49.
- Preserved existing application keys unchanged and saved new backup/privacy/storage keys directly into the owner's protected recovery note without secret values in chat or project files. Note lock was re-closed and verified; independent retrieval from another device is still awaiting the owner.
- Eight new settings are Staged in Fly; six existing credentials remain Deployed. No restart, migration or v2 deployment occurred.
- One actual release13 SQLite snapshot was AES-GCM encrypted, uploaded to Tigris, downloaded and restored in an isolated copy on Fly, with checksum/integrity/FK and signed-manifest readback passing. Artifact `pagnetic-6c2e74ce-63f1-4bdb-af82-9ff84a7d91a4.sqlite.enc`,1,245,184 bytes. No plaintext production database was downloaded to the local Mac (FileVault is off).
- Scheduled backup activation, independent key retrieval, legacy privacy receipt coverage, full application RTO, alerting and remaining launch acceptance are not established by this successful one-shot backup. Do not mark the launch goal complete.

### 2026-09-05 — Final correction acceptance and deployment handoff

- Astra independently verified the final source manifest, reran the exact completed-delivery replay reproduction and passed all25 targeted recovery/privacy/backup/access tests. A01–A03 are accepted; details in docs48. Sol is finished and its completion monitor is removed. No additional code batch or repeated full release audit is outstanding.
- The existing Fly organization has no listed Tigris buckets. Updated docs16 with confirmed existing owner inputs, exact remaining external configuration and ordered deployment/live acceptance steps. This check created no provider resource, changed no production secret and incurred no new service subscription.
- Launch remains incomplete. The next indispensable input is approved storage/key custody/alerting configuration; after that, actual recovery, deployment and Shopify/browser acceptance must still be performed. Do not restart Sol or repeat unchanged tests while waiting for this external input.

Append dated entries with package/requirement IDs, changes, tests, unresolved issues and next action. Do not replace the audit's historical evidence with a newly passing test result.

### 2026-09-05T08:20Z — S01 verified locally

- Scope: S01; partial R04/R05/R06/R08/R10/R11/R12/R14; local AT21/AT29 and partial AT25.
- Changed: `prisma/schema.prisma`, `prisma/migrations/20260905120000_mvp_v2_foundation/migration.sql`, `.env.example`, `app/services/mvp-v2.ts`, `app/services/job-outbox.server.ts`, `app/services/autopilot-presentation.ts`, `app/services/autopilot-presentation.server.ts`, `app/routes/app._index.tsx`, `tests/autopilot.test.ts`, `tests/mvp-v2-foundation.test.ts`, and this tracker.
- Behavior: v2 is default-off; additive storage does not rewrite legacy registrations; canonical tenant-scoped jobs/outbox reject changed idempotent input and stale leases; merchant status owns its CTA and blockers; measurement copy no longer contradicts blocking notices.
- Fresh verification using bundled Node 24.19.0 and pnpm 11.19.0: targeted S01 tests 13/13; full `pnpm check` 84/84 plus typecheck, lint, React Router production build and `shopify app build`; Prisma schema validation/generation; migration-to-schema diff returned “No difference detected.”
- Corrected during verification: an initial upgrade test used ISO text dates in a Prisma SQLite fixture and failed conversion. The fixture now uses the native integer-millisecond representation; the targeted and full checks pass. An initial Shopify build under the shell's Node 20.18.3 failed because the CLI requires `node:module.enableCompileCache`; the same build passed under the repository-pinned Node 24.19.0.
- External verification: none. No Fly/Shopify deployment, production database migration, commercial publication, real message, or provider charge was performed.
- Next safe package: S02, beginning with consent-aware visitor identity and atomic product deployment pointer. It was intentionally not started in this S01 checkpoint.

### 2026-09-05T08:29Z — Independent queue recovery review

- Fresh adversarial tests reproduced two gaps after the S01 checkpoint: expired lease holders could acknowledge completion, and workers crashing before recording failure could be reclaimed indefinitely.
- Fixed `app/services/job-outbox.server.ts`: completion/failure/delivery require live leases; failure writes recheck lease ownership atomically; both claimers bound total attempts including crashes; `failOutboxEvent` supports explicit bounded backoff and dead-letter without marking delivery successful.
- Added `tests/job-outbox-recovery.test.ts` with four database-backed regression scenarios, including transaction rollback and stale-worker rejection. These cover part of R11/AT25 and do not certify external alert/backup operation or PostgreSQL concurrency.
- Node24 targeted recovery/foundation tests: 7/7 passed after the two initial failures. Focused ESLint passed. Full integrated checks will run with the next coherent S02 checkpoint.
- Goal remains active. S02 is being built by Sol; root is reviewing reliability foundations. No production changes from this review.

### 2026-09-05 — Alert delivery and S02 independent review

- `app/services/alert-delivery.server.ts` now records v2 notification attempts per incident in the outbox, sends only to the configured HTTPS endpoint, requires HTTP2xx, reuses a stable delivery idempotency header on retry and skips resolved/replaced incidents. Receiver support for that header remains an external integration requirement; exact-once remote effects are not claimed.
- `app/services/automation.server.ts` uses the delivery adapter, records its outcome, attempts alerts even on the maintenance failure path, and resolves the previous automation-failure incident after maintenance recovers. Legacy delivery also rejects HTTP failures.
- Outbox claims can be scoped by merchant and event type, so alert processing cannot claim unrelated deployment events. Queue/delivery tests: 11/11 passed on Node24 with focused lint passing before the final maintenance-path wiring. Combined checks remain coordinated with Sol's next checkpoint.
- Independent S02 code review identified missing control-arm bridge events, incomplete revocation handling, broad form tagging and stale authority/expiry paths. Findings were sent to Sol for correction and regression evidence. S02 remains IN_PROGRESS until those checks pass; no launch gate is claimed.

### 2026-09-05 — Preparation-status regression and S03 source contract

- Reproduced and fixed an initial-state branch that hid PREPARATION_FAILED and CANDIDATE_TIE notices before a plan existed. `autopilot-presentation.ts` now surfaces those blockers and their owner/action; a new `autopilot.test.ts` regression proves the behavior.
- Relevant combined tests after final alert failure/recovery wiring and presentation fix: 23/23 passed on bundled Node24. Full application checks remain coordinated with Sol's corrected S02 checkpoint.
- Added `docs/27-v2-financial-field-contract.md` from official Shopify API documentation and current code inspection. It identifies line discount/refund double-counting pitfalls, transaction settlement authority, complete-read requirements, exact normalized money contracts and required fixtures. No v2 financial adapter or real-store tax/refund verification is claimed from that research.

### 2026-09-05 — S02 code-controlled checkpoint

- Scope: S02; partial R02/R04/R05/R12/R13/R14 and local portions of AT01–AT04/AT13–AT14.
- Changed: `prisma/schema.prisma`, `prisma/migrations/20260905120000_mvp_v2_foundation/migration.sql`, `app/services/v2-deployment.server.ts`, `app/services/v2-decision.server.ts`, `app/routes/storefront.experience.v2.decision.ts`, `storefront/adaptive-panel.js`, `storefront/adaptive-panel-v2.js`, built theme assets, `extensions/adaptive-panel/blocks/adaptive-panel.liquid`, `extensions/adaptive-measurement/src/index.ts`, `tests/adaptive-panel.test.js`, `tests/mvp-v2-runtime.test.ts`, `package.json` and this tracker.
- Behavior: v2 stays default-off. Consented visitors receive one fixed-expiry shop token and one fixed seven-day experiment assignment; raw identity is tenant-hashed. Both measured arms publish a decision and receive a product/variant-scoped signed cart reference. Frozen experiment policies govern serving. Closed enrollment retains existing in-window cohorts, while new/expired visitors receive the registered baseline without enrollment or renewal. Transactional authority leases order decisions against kill, product/source, pointer and lifecycle changes; pause fails open to Original.
- Review corrections: the first implementation omitted control-arm bridge events, did not fully handle revocation, tagged unrelated quick-add forms, read authority outside its write transaction, replayed expired treatment, dropped existing cohorts at enrollment close and derived serving from the deployment instead of frozen experiment policies. Focused regressions now cover each code-controlled contract. The combined check also revealed the main theme script exceeded Shopify's 10,000-byte app-block threshold; legacy and v2 runtimes are now independently built at 9,992 and 9,435 bytes.
- Fresh verification on bundled Node 24.19.0: S02 targeted tests 18/18; typecheck and focused ESLint pass; `shopify app build` passes without Theme Check errors; Prisma migration-to-schema diff says “No difference detected.” The combined suite reached 106/106 and typecheck before an empty-catch lint failure; that failure was fixed and focused lint passed. A final combined rerun remains coordinated with concurrent S08 changes.
- External verification: none. Shopify app-proxy POST transport, real Customer Privacy API events, published-theme block acknowledgment, mixed checkout paths, visible-page lease timing and real line-property retention remain S10 evidence. No production/Fly deployment, remote migration, commercial change or provider charge occurred.
- Next safe package: S03 durable webhook inbox, canonical financial ledger and assignment-line attribution, using the frozen field contract in `docs/27-v2-financial-field-contract.md`.

### 2026-09-05T09:03Z — S08 encrypted backup/restore implementation and follow-up runtime review

- Added streaming SQLite snapshot encryption, S3-compatible object upload/readback, authenticated checksum/version/schema manifests and isolated restore verification in `scripts/lib/sqlite-backup.ts` and `scripts/lib/backup-s3.ts`. Dedicated backup configuration uses `@aws-sdk/client-s3`; no provider resource was created or connected.
- Replaced the backup/restore shell internals with pinned-runtime TypeScript entry points. Recovery exclusively creates a NEW destination and cannot overwrite an existing database. Failed/corrupt uploads cannot publish latest verified evidence. Absolute SQLite paths avoid Prisma/CLI relative-path disagreement.
- Production readiness checks keyed manifest integrity, actual encrypted artifact bytes, configured off-volume destination and ≤24-hour snapshot/readback/restore freshness instead of accepting an arbitrary `.sqlite` filename. Configuration alone is not a passing backup gate.
- Supervisor attempts backups every six hours. Removed the broad arbitrary-directory retention deletion command; no material existing artifact was deleted. Provider retention and bounded local retention/disk monitoring remain required operational work, explicitly recorded in `docs/28-encrypted-backup-and-recovery.md`. Existing old-format backups remain untouched.
- Fresh Node24 checks: initial six backup tests plus runtime/presentation/queue/alert suites passed 44/44; focused lint passed. Expanded backup suite then passed 8/8, adding production-shaped evidence-checker assertions and foreign-key violation rejection. All object storage in these tests is an isolated fixture; this is not real S3, key escrow, alert delivery or full application RTO certification. Full integrated checks remain coordinated with Sol.
- Follow-up S02 inspection found additional lease/consent races not covered by its first checkpoint: 30-second polling plus response latency exceeds the visible-panel lease target; revocation/regrant during an outstanding request needs an epoch/abort guard; stale visibility listeners require cleanup. Sent to Sol with required deferred-fetch/fake-clock regressions. A/A analytics must join to assignment arm rather than infer arm from serving policy. S02 remains IN_PROGRESS.
- No live deployment, production database mutation, vendor provisioning or commercial publication occurred. The durable goal remains active; Sol continues S03 while the runtime findings and operational gates stay open.

### 2026-09-05 — Read-only Fly configuration refresh

- `fly status --app pagnetic --json` confirms deployed release13 with one started 1GiB application machine in Frankfurt and a passing service check. `fly volumes list` confirms a 5GiB encrypted volume, automatic snapshots and 14-day snapshot retention. These are current infrastructure facts, not v2 launch evidence.
- `fly secrets list` inspected names/status only, not values. No dedicated backup encryption/S3 credentials or alert webhook secret is present; machine nonsecret configuration likewise has no off-volume backup endpoint/bucket. Existing assignment, automation, field encryption, funnel and Shopify secret names are deployed.
- New backup/startup code must not be deployed before real backup configuration or an explicitly reviewed compatibility path is available: an unconfigured off-volume job correctly fails. Do not regress existing nightly backups while v2 is disabled. This is a deployment dependency, not a reason to stop independent code work.
- No resource, secret, machine, volume, snapshot or running configuration was changed by this refresh. Full v2 local tests continue; production remains on its prior release.

### 2026-09-05 — S09 provider-specific migration and real PostgreSQL rehearsal

- Added `scripts/lib/postgres-transfer.ts`, `scripts/prepare-postgres-track.ts`, `scripts/rehearse-postgres.ts`, `tests/postgres-transfer.test.ts`, the separate `prisma/postgresql` track and `docs/29-postgresql-migration-rehearsal.md`. Canonical SQLite schema/migrations and its generated default client were not changed by root.
- Fresh rehearsal on existing local PostgreSQL16.15: all58 modeled tables match canonical hashes/counts;15 populated source fixture rows include legacy approval/registration/report identity, exact legacy decimal sales/refunds, BigInt Shopify identity and3,000,000,000 v2 minor-unit strings. Twelve subsequent queue jobs were claimed once across two Prisma clients; cross-tenant/repeated completion rejected. A populated target rejects another transfer without changing its rows.
- The actual separate PostgreSQL migration history is applied and recorded, then `migrate deploy` and an empty schema diff verify it. Dump/restore into another new local database matches58 modeled table hashes and validated FKs. Exact timings and source/provider migration hashes are in `docs/audit-2026-09-05/postgres-rehearsal.json`; these small-fixture timings are not full application RTO or managed PITR evidence.
- Initial client-generation failure was fixed by locating the isolated generated client within the project's dependency resolution tree. Expanded fixtures exposed that empty-table parity alone would not cover legacy Decimal values; the adapter now explicitly handles them and actual money rows are checked. No precision repair/reclassification of historical reports is claimed.
- Transfer/type-focused unit tests and pinned typecheck/focused lint are part of the current checkpoint. Combined full application checks remain coordinated with Sol's S03 work. S09/AT26 stay IN_PROGRESS for provider packaging, full lifecycle contention, representative2x mixed load, intended-environment recovery and operational authority.
- Only owned temporary fixture clusters, generated clients and scratch data were removed after rehearsal. Production databases, Fly machines, vendors and commercial configuration were untouched. This turn is concrete progress toward the unchanged active goal.

- Follow-up transfer review fixed timezone-less SQLite `CURRENT_TIMESTAMP` interpretation: it is treated as UTC, not operator-local time. Added a legacy Decimal text-roundtrip guard so an inexact cast blocks transfer instead of silently changing money. Fresh `TZ=Asia/Jerusalem` unit tests passed3/3, the real PostgreSQL transfer/history/restore rehearsal passed again against the current schema, and pinned typecheck/focused lint passed. Combined backup/transfer unit checks previously passed11/11. Exact current rehearsal timing is in its generated evidence JSON.

### 2026-09-05T09:17Z — S03 code-controlled financial checkpoint

- Scope: S03; partial R06/R07/R11/R12/R14 and local portions of AT08–AT12.
- Changed: `prisma/schema.prisma`, `prisma/migrations/20260905120000_mvp_v2_foundation/migration.sql`, `docs/27-v2-financial-field-contract.md`, `app/services/financial-v2.ts`, `app/services/financial-ledger-v2.server.ts`, `app/services/shopify-financial-v2.server.ts`, `app/services/webhook-inbox-v2.server.ts`, `app/routes/webhooks.orders.create.tsx` plus the order/refund re-export routes, `tests/financial-v2.test.ts`, `tests/shopify-financial-v2.test.ts`, corrected S02 runtime/test assets and this tracker.
- Behavior: v2-enabled authenticated webhooks durably store a tenant-scoped, PII-minimized event and enqueue reconciliation before acknowledgment. Processing fetches the parent order, rejects tenant mismatch, records retry failure, normalizes exact shop-money strings and uses source timestamps/checksums for replay precedence. The 2026-07 adapter paginates line items and each refund child connection, requires exact transaction counts and rejects order/refund watermark drift. Signed assignment references are verified against tenant, product, decision, deployment and the half-open purchase window. Invalid references quarantine the order and audit the reason; corrected source snapshots supersede removed lines/refunds and rebuild visitor outcomes instead of retaining stale money.
- Financial fixtures: paid/authorization/partial/test/cancelled-paid, allocated discounts, settled/pending/split refunds, refund-first parent recovery, mixed/gift-card-product lines, incomplete/partial GraphQL reads, tax-inclusive hold, duplicate replay, late receipt, exact-expiry rejection and forged/cross-tenant references. A real migrated database roundtrip preserves `3000000000` minor units in v2 String columns, above PostgreSQL INTEGER32, without rounding or coercion.
- Fresh verification with bundled Node 24.19.0: financial/adapter tests 14/14; S02 runtime tests 20/20; `pnpm check` passes 133/133 plus typecheck, full ESLint, React Router production build and `shopify app build`; Prisma migration-to-schema diff says “No difference detected.” The first integrated run reached 133/133 but found an unbound `setImmediate` lint reference in the adversarial browser test; using the explicit `globalThis` reference fixed it and the complete rerun passed. Consent/lease race fixes were minified through the source build to 9,999 bytes without hand-editing generated assets.
- External verification: none. The adapter query is based on current official Shopify field contracts but has not been executed against the intended store. Tax-inclusive refund basis, native and accelerated checkout line-property retention, real reordered webhooks, production worker scheduling and post-checkout reconciliation remain required. No Fly/Shopify deployment, production migration, commercial change, real message or provider charge occurred.
- Next safe package: S04 cohort lifecycle, feasibility, analysis and truthful report projections. S03 remains IN_PROGRESS until the named Shopify/S08/S10 integration evidence is obtained; independent S04 work can proceed against the locally verified contracts.

### 2026-09-05 — Independent S03 adversarial corrections and PostgreSQL race

- Added `tests/financial-adversarial.test.ts`. The first six regressions all failed against the prior S03 checkpoint; two further tests reproduced incomplete-read recovery/conflict defects. Fixed source hashing so observation timing and local completeness/verification metadata are not source mutations, deterministic nested collection order, shop-currency validation across money fields, and rejection of conflicting duplicate transaction IDs.
- `financial-ledger-v2.server.ts` now supersedes stale/ineligible attribution authority, refuses signed decisions issued after purchase, distinguishes complete retries from incomplete reads at one watermark, and retains genuine equal-watermark quarantine until a newer authoritative revision. The expanded ten-case adversarial suite includes newer partial-state recovery and real-conflict persistence.
- `scripts/lib/financial-concurrency-rehearsal.ts` reproduced an older worker overwriting newer committed order facts on actual PostgreSQL. Conditional source-row locking and create-not-upsert now reject that stale writer; assignment outcome refreshes also lock before reading contributions. The full temporary PostgreSQL migration/concurrency/restore rehearsal passes again and its JSON evidence contains the controlled financial interleaving result.
- Pinned checks: financial/adversarial/Shopify-adapter tests pass 24/24; typecheck and focused ESLint passed on the implementation before the final two test-only additions. Sol was notified that root-owned financial paths are settled for the integrated S04 run. No schema, production or commercial state changed.
- S08 gaps communicated to Sol: a crashed PROCESSING inbox item currently needs lease/reclaim recovery, and an incomplete or pending canonical financial result must schedule reconciliation rather than disappear as a successfully completed event. These remain explicit unfinished worker requirements, not hidden launch-ready claims.
- Previous goal turn and this turn are progress, not blocked waits. The full goal remains active; S04 is being implemented and independently reviewable next.

### 2026-09-05T09:41Z — S04 lifecycle, qualification and report checkpoint

- Scope: S04; partial R02/R05/R07/R14 and local portions of AT05–AT07/AT15–AT16/AT22.
- Changed: `app/services/experiment-analysis-v2.ts`, `app/services/experiment-lifecycle-v2.server.ts`, `app/services/qualification-v2.ts`, `app/services/qualification-v2.server.ts`, `app/services/experiment-report-v2.server.ts`, `tests/experiment-v2.test.ts` and this tracker.
- Behavior: baseline qualification fails closed unless all frozen observation floors pass, persists a reproducible tenant/product-scoped snapshot and uses the larger analytic/seeded-bootstrap target with a conservative full-horizon forecast. Enrollment closes once at the target after the minimum duration or at the frozen day-42 deadline, and derives attribution/financial dates from actual assignment expiries. Analysis includes every assigned visitor with missing outcomes as zero, uses unbiased arm variance and a Welch/Student-t interval, and gives lifecycle/data/health insufficiency precedence over any effect label. Final reports are immutable, currency/date/scope bounded and suppress monetary claims for A/A, interrupted, invalid or insufficient evidence.
- Fresh verification on bundled Node 24.19.0: S04 focused tests 6/6; `pnpm check` passes 149/149 plus typecheck, full ESLint, React Router production build and `shopify app build`. The current schema/migration and legacy/v2 theme hashes remain unchanged from the release baseline; generated assets remain 9,992/9,999 bytes.
- External/integration verification: none. Required statistical multi-regime calibration is independently underway; production lifecycle scheduling, S05 health denominators, S06 merchant result screens and intended-store evidence remain open. No Shopify/Fly deployment, database mutation, vendor provisioning, commercial publication or real message occurred.
- Next safe package: S05 health contracts and denominator projections while S04 remains IN_PROGRESS until calibration and its named integration dependencies are complete.

### 2026-09-05 — Independent S04 statistical calibration and guards

- Added a development-only SciPy/NumPy reference harness, a persistent bridge to the actual TypeScript estimator and `docs/30-v2-statistical-calibration.md`. Machine evidence is `docs/audit-2026-09-05/statistical-calibration.json`, including source hashes and a passed unchanged-source check.
- Passed 45 Student-t quantile comparisons, 24,000 null experiments across 12 sparse/heavy-tail/repeated-session regimes and 12,000 positive-effect fixed-visitor bootstrap experiments across six regimes, with additional negative-effect counterfactual checks. Null false-positive rates were 2.30–5.25%; every Monte Carlo interval passed the documented inflation guard. Independent power was 88.25–89.95%, with all exact lower confidence limits above 80%; four example baselines still correctly remain traffic-infeasible PREVIEW_ONLY.
- Reproduced and fixed invalid NaN/median-degree inputs. Analysis now explicitly rejects malformed gates/dates/currencies/counts/arms and cannot validate or win with a zero-variance arm. Qualification v2.2 records and requires a conservative simulation-power lower bound rather than a point estimate, accounts for up to seven candidate sizes and rejects nonfinite/undersimulated inputs. No live experiment threshold changed.
- Focused S04/adversarial tests passed 11/11; full lint passed. Full typecheck overlapped active S05 edits and failed on the new ungenerated CampaignMapping fields and diagnosis/preview union types; Sol has the exact failures for its next integrated checkpoint. No claim of a new full green build yet.
- Remaining integration findings passed to Sol: checkout information floor must affect qualification/forecast, and a PostgreSQL enrollment close must lock before reading the final assignment set. Root will verify the latter independently. No production, customer, schema or commercial state was changed by this statistical slice.

### 2026-09-05T09:58Z — S05 source-backed diagnosis checkpoint

- Scope: S05; partial R01/R03/R12/R14 and local portions of AT17–AT18.
- Changed: `app/services/message-diagnosis-v2.ts`, `app/services/message-diagnosis-v2.server.ts`, `app/services/public-preview.ts`, `app/services/public-preview.server.ts`, `app/services/governance.server.ts`, `app/routes/_index/route.tsx`, `app/routes/_index/styles.module.css`, `app/routes/app.governance.tsx`, `app/styles/governance.module.css`, `prisma/schema.prisma`, new additive migration `prisma/migrations/20260905130000_campaign_evidence/migration.sql`, `tests/message-diagnosis-v2.test.ts`, `tests/public-preview.test.ts`, `docs/31-message-quality-evaluation.md` and this tracker.
- Behavior: the public preview now accepts optional exact campaign/ad text up to 2,000 characters, preserves entered values on errors and records only categorical funnel metadata. Without ad evidence it returns a clarity review; with evidence it returns a campaign-message review. The deterministic default extracts versioned exact source spans, selects one supported gap, creates at most one primary and one materially distinct alternative, or abstains with a concrete source request. It excludes instruction-like/high-risk spans, unsupported locale/category and contradictions. New authenticated campaign mappings freeze an exact evidence reference/hash separately from normalized UTM labels; evidence-backed diagnoses and drafts are tenant/product scoped and idempotent. Exact-body relabeling across angles cannot create a second draft.
- Evaluation: a 50-case automated matrix covers low-risk accessory opportunities, irrelevant/unsupported ads, repeated/short source, unsupported locale and instruction-like text, with targeted contradiction/category/high-risk/tenant cases. Every generated statement must equal an extracted product source span. This proves invariants, not merchant usefulness; the human ≥80% useful/clear target and zero-fabrication review remain unverified and are documented in `docs/31-message-quality-evaluation.md`.
- Fresh verification on bundled Node 24.19.0: S05 focused diagnosis/governance/public tests 15/15; Prisma generate/validate and migration-to-schema diff pass; `pnpm check` passes 160/160 plus typecheck, full ESLint, React Router production build and `shopify app build`. The new schema and campaign migration hashes are recorded in the release baseline; theme assets remain unchanged at 9,992/9,999 bytes.
- External verification: none. No public preview request, real merchant ad evidence, Shopify/Fly deployment, production migration, vendor/model call, commercial publication or message occurred. The optional model remains default-off. S05 stays IN_PROGRESS for independent human content review and S06 merchant workflow evidence.
- Next safe package: S06 actual review/enablement and complete ordinary merchant screens. The separate observable-checkout forecast floor and frozen financial-cutoff issues remain open dependencies; neither is hidden by this checkpoint.

### 2026-09-05 — S04 enrollment-close race and S05 PostgreSQL refresh

- Added `scripts/lib/lifecycle-concurrency-rehearsal.ts` to the actual PostgreSQL rehearsal. The first controlled interleaving failed: close froze attribution at `2026-09-08T00:00:00.000Z` although a concurrent assigned visitor required `2026-09-21T23:59:59.000Z`. The assignment writer uses the same experiment-row lock as serving.
- Corrected `closeV2EnrollmentIfDue` to acquire that lock before reading assignments. The rerun includes both visitors, freezes the correct attribution and financial cutoffs, preserves the financial stale-writer regression, and passes migration/readback/job-concurrency/dump-restore checks. Current machine evidence records `lifecycleConcurrency.concurrentAssignmentIncluded=true` and `schemaStillCurrent=true`.
- Refreshed/reviewed the separate PostgreSQL track with additive migration `20260905095457_d5d5f0ce9629`: nullable campaign evidence reference/hash and legacy-safe default-en locale. No existing provider migration was rewritten and no live database changed.
- Fresh typecheck, full no-cache ESLint and focused 11-test S04/adversarial run pass. A parallel cached ESLint invocation first hit a cache `hashOfConfig` error; the uncached rerun passed. Final independent statistical rerun also passed against unchanged current source hashes. Sol is obtaining the integrated S05 checkpoint before S06 work.
- New read-only P0 follow-up, not yet fixed: visitor projection currently counts settled refunds without enforcing the frozen financial cutoff, and final report calls can recompute from mutable projections. Root will implement immutable financial fact/as-of report handling; S03/S04 remain open. This is ongoing engineering progress, not an external blocker or launch-ready claim.

### 2026-09-05T10:15Z — S06 ordinary merchant UI checkpoint

- Scope: S06; partial R03/R07/R08/R12/R14 and local portions of AT19–AT22/AT28. Changed `app/routes/app.tsx`, `app/routes/app._index.tsx`, `app/routes/app.messages.tsx`, `app/routes/app.preview.tsx`, `app/routes/app.results.tsx`, `app/routes/app.settings.tsx`, `app/routes/app.settings.export.ts`, `app/services/autopilot-presentation.server.ts`, `app/services/governance.server.ts`, `app/services/pilot-setup.ts`, `app/services/result-presentation-v2.ts`, `app/styles/governance.module.css`, `tests/merchant-ui-v2.test.ts`, `tests/partner-readiness.test.ts` and this tracker.
- Merchant workflow: default navigation is Overview, Messages, Results and Settings. Messages shows actual synced catalog source beside diagnosis/draft, supports exact campaign evidence, edit/revalidation and source trace; Preview compares current source reconstruction with the proposed panel at desktop/mobile widths and links separately to the real product page. The theme editor deep link carries the selected handle and actual template suffix. V2-edited drafts retain the two-to-four-benefit validation rules rather than falling back to the legacy three-benefit contract.
- Result integrity: Results reads only the exact `Experiment.finalResultSnapshotId`. A provisional/latest-but-unpinned snapshot cannot produce a claim; snapshot/payload state mismatch, non-effect states and noninteger money suppress the monetary headline. A new immutable financial revision after finalization appears as a separate Pagnetic-review notice and cannot overwrite the frozen report. The legacy Overview cannot reinterpret a v2 snapshot with legacy field semantics and routes the merchant to Results instead.
- Settings/safety: active serving exposes immediate pause; subscription wording is based on stored authoritative provider status and does not treat a favorable result as payment approval. Surface evidence shows applicability and explicit expiry, incidents identify Pagnetic ownership, and the authenticated export excludes raw visitor identities, customer/order payloads and secrets. No canonical financial revision payload is queried or rendered by these UI routes.
- Fresh verification on bundled Node24.19.0/pnpm11.19.0: focused S06/readiness/presentation tests pass25/25; focused typecheck and uncached ESLint pass. Integrated `pnpm check` passes175/175 plus typecheck, full ESLint, React Router production build and `shopify app build`. Built theme assets remain 9,992/9,999 bytes with hashes `2430605bba81f195c03fb525124d6fd8eea33db1b138a4ed9df653eb8a1a9c59` and `a946bb05fadcba246bfd337bfd2baa6a7057d65b50012710ee1460c31d484f1a`.
- Not claimed: no browser screenshot/keyboard/screen-reader/zoom check, authenticated product-specific runtime acknowledgment, duplicate-block/Save-inactive recovery, Shop Pay applicability check or intended-store observation was performed. Exact keep/revise/stop receipts remain S07. No Shopify/Fly deployment, production migration, vendor/resource purchase, offer publication or commercial change occurred.
- Next dependency package is S08 worker/recovery completion, then S07. S06 remains IN_PROGRESS until the named browser/integration evidence exists; the code-controlled checkpoint is green.

### 2026-09-05 — Independent S03/S04 immutable financial cutoff and finalization

- Added `FinancialOrderRevision`/`FinancialRevisionLink` and `Experiment.finalResultSnapshotId` through new SQLite migration `20260905140000_financial_revisions`. Refreshed and reviewed separate PostgreSQL migration `20260905101252_61b881806941`; existing histories remain untouched. The schema/migration diff passes.
- Financial v2.2 retains sanitized immutable revisions, including stale-but-historically-relevant facts, and verified context separately from current ledger projections. Refund transaction processing times are now requested and retained; split settlement uses the last successful component. New as-of service resolves source facts through the frozen cutoff and reports explicit gaps/conflicts rather than guessing historical money.
- Finalization and financial writers share the experiment-row lock. The snapshot and final pointer are written in one coherent transaction, with tenant-scoped pinned replay. Later revisions retain audit evidence and cannot silently rewrite the report; S06 surfaces a separate review notice. Existing linked v2 rows with missing immutable history cannot be treated as reconstructed facts.
- Targeted as-of/adversarial tests pass21/21, including two actual SQLite immutable-history/report tests. Previous combined financial/lifecycle run passed36/36. Typecheck, focused uncached ESLint and migration diff pass. Sol's latest integrated S06 checkpoint passes175/175 plus app/Shopify builds.
- Actual PostgreSQL report/fact interleaving passes: pre-cutoff concurrent update is included at8,000 minor units, a later operational change reaches4,000 without changing report ID/payload. Migration/readback/job/financial/lifecycle/report concurrency plus dump/restore pass across60 tables. The first new fixture run lacked the required explicit Decision ID; adding that fixture ID resolved it before the passing production-path rehearsal.
- Detailed contract and limitations: `docs/32-financial-as-of-and-frozen-reports.md`. No live/deploy/commercial change. Intended-store timing/refunds, post-final operator alerts and targeted privacy cleanup remain integration work. Sol owns S08 inbox leases/retry and may now add its next migration; root will independently finish health-denominator/forecast gaps next. The full goal remains active and this turn is engineering progress, not a blocked wait.

### 2026-09-05T10:42Z — S08 inbox recovery and privacy checkpoint

- Added lease token/expiry authority to `WebhookInbox` through additive migration `20260905150000_webhook_inbox_leases`. Expired PROCESSING work is reclaimable; lease renewal occurs after a remote fetch and before state mutation; a replaced worker cannot reconcile or acknowledge. Incomplete/conflicting canonical reads remain RETRY and enqueue a deterministic follow-up instead of disappearing as processed. Exhausted crash attempts become one DEAD_LETTER plus an in-app incident.
- Added a tenant/type-scoped financial reconciliation worker and invoked it from authenticated maintenance only when v2 and its signing authority are configured. Missing v2 signing configuration does not break legacy maintenance but creates an operator-owned alert. Post-final financial revisions surface as operator review alerts without changing the frozen result.
- Shop redaction explicitly inventories immutable financial revisions/links before merchant-cascade deletion; customer redaction truthfully records that no customer join is stored. New tests cover expired reclaim, stale fetch interleaving, incomplete follow-up, worker scoping, dead letter and privacy deletion.
- Focused S08/privacy/backup/result/runtime tests pass38/38; the later integrated S07 run passes202/202 plus typecheck, ESLint, production app build and Shopify build. Actual scheduler, remote alert destination, backup credentials/off-volume readback and retention execution remain unverified; no production or external state changed.

### 2026-09-05T10:48Z — S07 result actions and subscription-authority checkpoint

- Added authoritative provider metadata to `SubscriptionState` through additive migration `20260905160000_subscription_authority`. Verification binds the provider shop to the authenticated merchant, hashes a bounded canonical payload, ignores older source updates and freezes access on contradictory equal-watermark facts. Decline/cancel/freeze mappings are explicit; only active, bounded free evaluation or a paid-through cancellation window can authorize approved serving.
- The offer catalog preserves existing $49 founding terms without new enrollment and defines Pagnetic Core at $99/month with a bounded measured evaluation. It remains unpublished unless both billing and offer-publication flags are explicit. Settings shows exact stored status/offer and states that no charge is created while publication is closed. No pricing page, provider plan or charge was created.
- Results now executes authenticated, role-checked keep/revise/stop commands with duplicate-submit protection and stable idempotency keys. Keep requires an exact positive final snapshot, non-A/A comparison, active entitlement and approved content frozen in that experiment registration; it atomically advances to nonexperimental approved serving. Revise creates one separate source-backed draft without rewriting the old report. Stop advances the product to an Original/STOPPED pointer while financial reconciliation remains independent.
- Verification exposed a real diagnosis/runtime hash mismatch: diagnosis candidate identity hashed a display body while runtime authority hashes the complete content object. Draft persistence now stores the exact runtime content hash, and experimental deployment also refuses content absent from the frozen registration. Targeted S07/runtime/diagnosis tests pass15/15; subscription tests pass4/4; broader S07/S08 suite passes38/38. Prisma validate and SQLite migration-to-schema diff pass.
- First integrated `pnpm check` reached201/202 and exposed that the legacy-upgrade fixture applied new dependent migrations before the v2 foundation. The fixture now seeds after pre-v2 history, applies foundation, then every additive migration in order. Fresh bundled Node24.19.0 `pnpm check` passes202/202 plus typecheck, full ESLint, React Router production build and Shopify build. No live Shopify/Fly deploy, billing callback, charge or offer publication occurred.
- S07 remains IN_PROGRESS for a real Shopify App Pricing adapter/no-charge dev path, reinstall and consumed-evaluation enforcement, and S10 storefront evidence. The separate PostgreSQL track must be refreshed for the two latest additive migrations before any provider cutover.

### 2026-09-05 — Independent capture, health and qualification checkpoint

- V2 pixel now enforces analytics plus preferences before context storage/delivery, serializes revoke cleanup against pending writes and invalidates queued/retrying events by consent epoch. Mixed checkout matching includes a focal product after the first line. The receiver validates active credentials, fresh new-event receipt, exact persisted decision context, tenant-HMAC identities and strict data allowlists. Atomic render/event persistence prevents contradictory final outcomes and duplicate coverage inflation.
- Added persisted `pagnetic-measurement-health-v2.1`: distinct bridge/render acknowledgment, observed missing-link checkout failures, separately reported unknown context and unassigned store orders, non-test 100-checkout-per-arm floor, financial reconciliation and contradictory-link guards. A versioned conservative prelaunch 95% overall checkout-linkage rule rejects symmetric severe loss. This is synthetic fault-injection evidence, not population calibration or real-store A/A validation. Final reports derive and pin health internally; caller READY cannot bypass it.
- Added v2.3 qualification checkout-rate planning and outcome-maturity guards. First-eligible daily counts must equal the distinct baseline sample. Missing checkout evidence gives no precise date; slow checkout rates extend both-stage forecasts or yield PREVIEW_ONLY. The 250-checkout-visitor buffer is conditional planning, not an A/B probability guarantee; actual 100-per-arm evidence is still required. Frozen statistical/maturity windows cannot be shortened to fit a trial.
- Fresh verification: actual-pixel VM privacy tests4/4; SQLite receiver/health-loader tests5/5; pure health tests6/6; combined report/financial-adversarial/health tests24/24; qualification/lifecycle/statistical-adversarial tests16/16; typecheck and focused uncached lint pass. The independent 45-quantile/24,000-null/12,000-power calibration rerun passes against unchanged v2.3 source hashes. Sol's last integrated checkpoint202/202 predates the final qualification additions; another combined run is required.
- Reviewed separate additive PostgreSQL migrations `20260905104535_e60dcd194232` (inbox lease/subscription authority) and `20260905105049_67e116f7c550` (evaluation consumption). Current-schema rehearsal passes61 tables,15 seeded rows,12 exclusively claimed jobs, financial/lifecycle/report interleavings and dump/restore readback. Sol's subsequent health-aware early-close changes will require refreshing the lifecycle regression evidence again. No live database changed.
- Remaining engineering: production mature baseline assembler, complete v2 merchant-plan creation/transition, health-aware early enrollment close, bounded persistent lifecycle job chain and intended-environment operational/browser evidence. Root passed the two lifecycle review findings to Sol. Pixel preferences-manifest deployment also needs a legacy active-experiment compatibility/cutover review. Details and limitations are in `docs/33-v2-capture-health-and-qualification.md`. Goal remains active; no production, billing, resource purchase or launch-readiness declaration occurred.

### 2026-09-05T11:03Z — S07 evaluation ledger and S08 lifecycle-worker hardening

- Added persistent `EvaluationConsumption` authority through additive migration `20260905170000_evaluation_consumption`. Atomic deployment authorization consumes one measurement check, one initial message test and at most one qualifying negative/inconclusive revision. New experiment or result IDs, uninstall and entitlement reconciliation cannot reset those unique allowances; paid ACTIVE/paid-through authority bypasses the free ledger without deleting it.
- Durable v2 lifecycle work now uses a stable per-experiment root key and successor keys derived from the predecessor. Maintenance sees completed and dead-lettered chain members, so concurrent enqueuers cannot create parallel roots and a dead letter cannot silently receive a fresh retry budget. The worker rechecks experiment state after claim: a serving pause explicitly closes the cohort as interrupted and still schedules financial maturation/finalization; stopped, invalidated or already-finalized work completes as `SKIPPED_INACTIVE` without mutating or re-enqueuing.
- Target/minimum-duration early close now also calculates persisted, deduplicated, consented, non-test checkout observations under the experiment row lock and requires at least100 outcomes in each frozen arm. Hard deadline and explicit owner/safety stop behavior remain unchanged. Shop redaction explicitly inventories and removes evaluation-consumption rows, while uninstall retains them as a commercial-abuse guard.
- Fresh bundled Node24.19.0 evidence: targeted lifecycle/experiment/privacy/uninstall tests pass24/24; integrated `pnpm check` passes213/213 plus typecheck, full ESLint, React Router production build and Shopify build. Built theme assets remain within limits at9.8KB/9.8KB/862B. No plan/bootstrap orchestration claim is made: lifecycle workers operate only on already registered v2 experiments. No production deploy, provider call, charge, offer publication or external state change occurred.

### 2026-09-05 — Original-only authoritative baseline assembly

- Added `snapshotOriginalBaselineV2` as an internal service. It requires a closed, mature registered v2 Original/Original source, exact in-scope full-day observation dates, eligible consented assignments and actual session authority. It rejects treatment contamination, interrupted/materially confounded sources, severe open incidents, capture invalidity, missing immutable financial facts and foreign tenant/product scope. The evidence snapshot shares the experiment lock with financial append and enrollment.
- Baseline outcomes include zero-purchase visitors and immutable net focal money; checkout forecast counts at most one verified paid non-test purchaser per visitor. A repeat order can increase money but cannot increase visitor or purchaser/checkout counts. No raw outcome array is returned. Frozen source hashes preserve idempotent qualification; a new source snapshot cannot rewrite an active experiment registration.
- `loadV2BaselineCapture` now explicitly returns `qualificationUsable=false`, a traffic-only source version and missing-session count. It no longer invents sessions by falling back to Shopify client ID. No historical unassigned view was backfilled into financial attribution.
- Fresh SQLite test uses2,100 assigned visitors and202 production financial normalizations/signed-reference reconciliations, including one repeated paid order and one excluded test order. Mature qualification/idempotency/counts/no raw arrays, immature rejection, tenant/treatment/date isolation, material-confounder/missing-session/capture-loss/missing-history guards pass. Combined baseline/runtime run passed10/10 before the final repeated-order assertion; that final source regression rerun also passes. Focused uncached lint passed; an initial concurrent typecheck saw Sol's temporary undefined `fixture` test reference, which its subsequent integrated213/213 checkpoint corrected.
- Refreshed actual PostgreSQL evidence passes61-table parity,12 exclusive job claims, financial/report interleavings and dump/restore. The two-visitor cohort-lock regression now explicitly closes at its hard deadline, preserving the concurrent visitor without pretending it meets the new early-close checkout floor. `schemaStillCurrent=true` and the close path are in the machine evidence.
- Handoff: Sol will connect Original-only baseline/A/A bootstrapping, mature snapshot qualification and effect registration to the actual merchant plan without circular qualification or automatic commercial activation. Root identified and passed a finalization-versus-serving-pause coupling issue; Sol's latest worker checkpoint now records interrupted closure with continued maturation. Remaining root work includes bounded streamed storefront request handling and independent lifecycle/merchant integration review. See `docs/34-original-baseline-source-contract.md`. Goal remains active; no live deployment, source mutation, paid offer or external resource change occurred.
- Final source checkpoint: fresh root `pnpm typecheck` and focused uncached ESLint pass after the completed baseline/confounder/repeated-order changes. No full combined-build claim is inferred from these narrower checks; Sol owns the next coherent integrated checkpoint with plan orchestration.

### 2026-09-05 — Streamed storefront request boundaries

- Replaced whole-body-then-size-check handling on pixel events and v2 decisions with a bounded streaming UTF-8 reader:32KiB/8KiB actual bytes,5s body deadline, abort/oversize cancellation, malformed/mismatched header and encoding rejection, plus a finite chunk/nonprogress bound. Unsupported mutation methods return405. No-store and pixel CORS remain; decision failures return Original.
- Six direct stream regressions and two actual-route-bundle tests pass8/8. External auth/database/business services are isolated test stubs; the real routes and body reader execute. Focused uncached ESLint passes after fixing two style findings; root also fixed a header-fixture type union. Full typecheck encountered Sol's transient new-orchestrator import before the file was created, so an integrated pass remains pending its coherent checkpoint.
- No live/deploy/commercial changes. Limits bound application accumulation and body-read duration, not upstream buffer allocation, authentication or database latency. Scope and remaining operations work are recorded in `docs/35-storefront-request-boundaries.md`.
- Independent plan review passed three integration findings to Sol: phase/pointer/evaluation activation must be atomic or equivalently staged/recoverable; legacy theme/QA booleans cannot replace fresh scoped v2 evidence; a day7+minutes baseline close cannot round incomplete cohort bins into seven full days or permanently strand a completed under-floor baseline. Root is continuing operations and integration review. Goal remains active and this turn made tested engineering progress.
- Subsequent pinned root `pnpm typecheck` passes after the new orchestrator source appeared. The eight boundary tests and focused lint remain green; no full-build or intended-environment approval is inferred.

### 2026-09-05T11:30Z — V2 plan orchestration and Shopify subscription adapter checkpoint

- Added immutable v2 experiment registration and connected the approved merchant plan to a fixed 14-day Original/Original baseline, exact pinned measurement result, authoritative mature baseline snapshot, frozen Original/Universal effect registration and exact final-result pointer. The fixed baseline deadline prevents a fractional last day from being silently counted or permanently stranding visitors outside the qualification window; target and message content are never invented.
- Baseline and effect activation recheck approval, frozen inputs, active product/pixel, published-theme identity, template applicability and fresh per-surface QA evidence. The effect transition additionally requires evidence for the exact prior deployment. V2 remains default-off and the legacy plan path is unchanged while the flag is closed.
- `installV2Deployment` now supports an in-transaction authority callback. Pointer revision, persistent evaluation consumption, experiment activation, plan phase, transition/audit, outbox and action receipt either commit together or all roll back. A reproduced async lock-finalizer race was fixed by awaiting the phase start before clearing the plan lease. A callback-failure regression proves that no pointer, deployment, consumption, receipt, outbox or runtime-control row survives rollback.
- Runtime validation now applies the already-approved two-to-four benefit rule to deterministic source-composer v2 content; legacy content retains its three-benefit minimum. Result discovery no longer depends on still-fresh serving evidence, so a matured frozen result remains reviewable after serving/theme evidence becomes stale.
- Added a bounded, read-only Shopify Partner API 2026-07 active-subscription adapter, strict Admin shop-ID verification and an OWNER/OPERATOR refresh action in Settings. Missing active subscription preserves the bounded free evaluation; transport, GraphQL, identity and shape errors fail closed. Pricing remains unpublished unless both existing commercial flags are explicit; the adapter never creates a charge.
- Changed: `app/services/experiment-registration-v2.server.ts`, `app/services/autopilot-v2-orchestrator.server.ts`, `app/services/autopilot-orchestrator.server.ts`, `app/services/autopilot-preparation.server.ts`, `app/services/experiment-lifecycle-v2.server.ts`, `app/services/v2-deployment.server.ts`, `app/services/runtime.server.ts`, `app/services/shopify-app-pricing-v2.server.ts`, `app/services/subscription-v2.server.ts`, `app/routes/app.settings.tsx`, `.env.example`, `tests/autopilot-integration.test.ts`, `tests/mvp-v2-runtime.test.ts`, `tests/runtime.test.ts`, `tests/subscription-v2.test.ts` and this tracker.
- Fresh bundled Node24.19.0/pnpm11.19.0 verification: focused runtime/deployment/autopilot tests29/29, focused typecheck and uncached ESLint pass; full `pnpm check` passes233/233 plus typecheck, full ESLint, React Router production build and Shopify build. Built assets remain9,992/9,999/862 bytes with hashes `2430605bba81f195c03fb525124d6fd8eea33db1b138a4ed9df653eb8a1a9c59`, `a946bb05fadcba246bfd337bfd2baa6a7057d65b50012710ee1460c31d484f1a` and `47cb08c2f6df419f6827bbfb62b7965bf59913add9ca10610b541ff5a6c98335`.
- No production deploy, live Partner API call, billing plan/charge, offer publication, customer message or external resource change occurred. Real credentials/no-charge plan observation, intended scheduler operation, browser/Shopify commerce evidence and the remaining S08/S09/S10 gates keep public launch readiness unclaimed.

### 2026-09-05T11:39Z — Local AT27 mixed-load and mobile-fault rehearsal

- Added `scripts/lib/mixed-load-v2.ts`, executable `scripts/rehearse-mixed-load-v2.ts`, `tests/mixed-load-v2.test.ts`, `docs/37-local-mixed-load-rehearsal.md` and hash-bound machine evidence `docs/audit-2026-09-05/mixed-load-v2.json`.
- The full local run applied current migrations to an isolated single-writer SQLite database and exercised four tenant-isolated registered v2 deployments. At twice the declared synthetic fixture peak it scheduled16 decisions,4 financial webhook accepts and2 report reads per second for five seconds. All110 operations completed with zero errors; decision p50/p95/p99 was10.066/16.774/26.566ms, there were zero unexpected fallbacks and every tenant completed20/20 decisions. Twenty reconciliation jobs remained intentionally queued, with oldest age6,445ms, and RSS start/peak/end plus dispatch lag are retained in the machine record.
- Four response deliveries delayed1,600ms behind the1,500ms client render deadline all returned Original. This client-boundary injection complements, but does not replace, the generated-runtime VM tests for stale/deferred fetches and consent generation.
- The smaller automated regression passes1/1; typecheck and focused uncached ESLint pass. A fresh integrated `pnpm check` passes235/235 plus typecheck, full ESLint, React Router production build and Shopify build; the count also includes the concurrently completed supervision regressions.
- AT27 remains IN_PROGRESS: rates are declared fixtures rather than observed cohort traffic; Prisma SQLite exposes no lock-wait metric; no real PostgreSQL/Fly/Shopify proxy, queue worker, production-shaped dataset, supported mobile browser or population Web Vitals were measured. No live system or external resource changed.

### 2026-09-05 — Production supervisor and bounded scheduled work

- Replaced the unsupervised shell background loops with a Node supervisor while preserving migration-before-app startup and the five-minute/six-hour schedule. Startup health is bounded at120s, each probe at5s, migration at10min, maintenance HTTP at90s and backup process at15min. Owned POSIX process groups receive TERM then bounded KILL, including descendants. Application exit or startup failure shuts down its owned work.
- Maintenance now rejects HTTP207 partial failures instead of treating any2xx response as success. An ambiguous maintenance network/deadline failure exits the supervised app for platform restart, preventing repeated requests from accumulating behind an operation that continues server-side after its caller aborts. HTTP-reported completed failures are logged for the next scheduled retry; persistent app incidents remain the notification authority. No secret or response payload is logged.
- Six tests pass using real subprocesses and loopback HTTP, including a real HTTP-serving grandchild terminated with its parent, stalled connection cancellation, sequential retry and startup deadline. Focused uncached lint, shell syntax and root typecheck pass. Sol's integrated233/233 checkpoint includes the currently settled supervisor source and both application/Shopify builds.
- No production entrypoint, live migration, external scheduler, backup or storage deletion was executed. Actual Fly restart behavior, representative workload duration, remote alerts/off-volume recovery and local authenticated backup retention remain required. Details are in `docs/36-production-supervision.md`. This is tested engineering progress; goal remains active. Sol is continuing isolated AT27 load/fault evidence while root owns remaining operations work and independent integration review.

### 2026-09-05T11:58Z — Local actual-component browser and accessibility checkpoint

- Refactored the authenticated Overview, Messages, Results and Settings routes to export their presentational views while preserving their ordinary loaders/actions and default wrappers. Added `qa.v2-ui.tsx` only as a PII-free local fixture harness; it requires non-production mode, an explicit `PAGNETIC_UI_QA=true` flag and a loopback hostname, returns 404 otherwise, marks itself noindex and rejects every action with HTTP405.
- In Chrome152 on macOS, the actual views were inspected through the accessibility tree and screenshots. Overview/Messages/Results/Settings exposed semantic h1/h2 structure, explicit control names, native disclosures and logical keyboard order with no trap. Positive Results exposed exact frozen money/evidence while correctly omitting Keep when subscription authority was absent. Settings exposed pause, bounded evaluation, unpublished-offer wording, per-surface evidence and the open financial-review incident.
- At 200% zoom in the available1,400×768 browser window, all four tested top-level states reflowed without visible horizontal clipping or overlap; vertical scrolling remained expected. This is useful local evidence, not the exact responsive/device matrix or a full screen-reader certification.
- Changed: `app/routes/app._index.tsx`, `app/routes/app.messages.tsx`, `app/routes/app.results.tsx`, `app/routes/app.settings.tsx`, `app/routes/qa.v2-ui.tsx`, `app/services/ui-qa-fixture.server.ts`, `tests/ui-qa-fixture.test.ts`, `docs/40-local-ui-accessibility-qa.md` and this tracker. Focused fixture tests2/2, typecheck and uncached ESLint pass; full integrated check follows after concurrent settled operations changes.
- No authenticated Shopify route, production/customer data, App Bridge, published storefront, privacy API, checkout, billing or merchant action was used. VoiceOver/mobile browsers, background-refresh edit retention and real embedded/storefront state remain AT28/S10 requirements.

### 2026-09-05T12:30Z — Exact responsive-width matrix

- Chrome152 DevTools responsive mode exercised the actual Overview, Messages, Results and Settings components at exact CSS widths320,375,768,1024 and1440. All20 cells reported `documentElement.scrollWidth == body.scrollWidth == innerWidth`; no horizontal overflow was present. Visual inspection at both extremes confirmed mobile stacking/wrapping and the wider grid/card transition without clipped actions or overlap.
- The1440px viewport was auto-scaled by DevTools to75% for the available display area while retaining an exact1440 CSS-pixel `innerWidth`. Observed console warnings were Chrome-extension content-script warnings, not app-origin exceptions. Evidence is recorded in `docs/40-local-ui-accessibility-qa.md`.
- This closes only the local exact-width portion of AT28. It does not certify embedded App Bridge, VoiceOver, supported mobile browsers, authenticated background edit retention or any published-storefront commerce behavior. No source/runtime, Shopify, production, customer, billing or external state changed during the read-only matrix.

### 2026-09-05T12:39Z — Visible preparation progress and uninstall/lease authority

- The Overview loader now returns the real durable-job pending state. While no plan exists, the client performs at most24 visible, idle revalidations at15-second intervals—enough to span the five-minute maintenance cadence—and stops immediately when the job becomes ready or terminal. It does not refresh while hidden, navigating, already revalidating or while an input/select/textarea/contenteditable control has focus, so an in-progress merchant edit is not overwritten.
- Catalog synchronization, plan preparation and Web Pixel persistence accept a worker authority callback. The worker rechecks the exact tenant job lease against a live clock before and after external waits; final plan and Pixel writes lock runtime authority then the exact lease in the same transaction. A stale/reclaimed worker cannot persist those serving mutations.
- Preparation authority now covers every local write unit without holding a transaction across Shopify GraphQL: plan creation, Pixel credential persistence and serving transitions are transactionally runtime/lease guarded; each fetched product's catalog/source/evidence persistence, final brand/audit, entitlement/candidate, notice, evidence approval, diagnosis and draft/claim persistence unit also rechecks that exact authority inside its own short transaction. A unit already holding the valid lock may commit before a concurrent uninstall/reclaim, after which uninstall neutralizes serving state; once lease replacement or uninstall wins the lock, the older worker cannot persist a later unit, approve, activate or serve. Read-only preparation can occur between units, but its result cannot cross a guarded write boundary without current authority.
- Uninstall now cancels only active `AUTOPILOT_PREPARATION` and enrollment-closing jobs, invalidates plan/runtime/Pixel state and blocks later preparation enqueue. Financial finalization and privacy jobs deliberately remain eligible after uninstall. This preserves mandatory reconciliation/privacy processing while preventing serving resurrection.
- Fresh focused evidence: preparation refresh/worker7/7, including transaction-client authority failure with zero product/source/evidence persistence and loss of authority between diagnosis and draft transactions with zero draft/claim persistence; combined preparation, measurement and autopilot integration33/33 before these two additional transaction cases; typecheck and focused uncached ESLint pass. Tests also suspend Shopify catalog/Pixel responses, reclaim a lease or uninstall mid-flight, then prove no catalog/plan/Pixel persistence in those external-response interleavings, no Pixel creation after uninstall and no reset of financial/privacy work.
- Changed: `app/routes/app._index.tsx`, `app/routes/qa.v2-ui.tsx`, `app/services/autopilot-preparation-refresh.ts`, `app/services/autopilot-preparation-worker.server.ts`, `app/services/autopilot-preparation.server.ts`, `app/services/governance.server.ts`, `app/services/measurement-reliability.server.ts`, `app/services/measurement.server.ts`, `app/services/uninstall.server.ts`, `tests/autopilot-preparation-worker.test.ts` and this tracker. The earlier246-test release manifest/build remains historical after these edits; no production, Shopify, customer, billing, secret or commercial state changed.

### 2026-09-05T12:46Z — Bounded consent-aware Web Pixel delivery

- Replaced the global promise chains with explicit finite queues: at most64 delivery tasks are concurrently retained including the active task, and at most16 decision-storage writes wait behind the active write. Delivery preserves accepted order; excess work is deliberately dropped rather than retained without bound. A dropped event is not relabeled as captured or reconciled.
- Every fetch attempt has a1-second deadline and an `AbortController` signal when the runtime provides it, with three bounded attempts and the existing short retry delays. Decision storage reads are bounded at250ms so a hung browser API cannot freeze delivery. Storage mutations are never released on a synthetic timeout because the browser API cannot cancel them: revoke clears, stale-write cleanup and fresh writes remain in one settled serialization chain. Persistent state is untrusted after a consent transition until its clear settles; a fresh in-memory decision can still support bounded delivery without exposing the stale value.
- Every Customer Privacy change increments the epoch, aborts the active request, resolves/drops queued delivery and storage tasks and clears stored context when either analytics or preferences is denied. A later grant cannot revive or retry old-epoch work; only a newly emitted event can send.
- Fresh VM regressions pass12/12: denied consent, mixed-product matching, revocation during storage, late old-write/fresh-epoch ordering, deferred revoke clear/fresh-write serialization, stale cleanup held beyond the former250ms deadline, revoke/regrant retry containment, hung-fetch abort/recovery, transient retry ordering, active+queued revocation, a200-event ordered burst capped to64 and a100-decision burst with one active+16 pending storage writes. Typecheck and focused uncached ESLint pass; Shopify build succeeds at7.5KB original/~3.0KB compressed.
- Changed: `extensions/adaptive-measurement/src/index.ts`, `tests/pixel-privacy-v2.test.js` and this tracker. No extension deploy, live consent event, customer data, external request, Shopify mutation or production state changed.

### 2026-09-05 — Authenticated cache retention and backup capacity preflight

- Added `scripts/lib/backup-retention.ts`: bounded generated-name discovery, keyed manifest and ciphertext verification, configured-destination binding, private directory/singly linked file checks, minimum two recovery copies and fresh retained-latest remote re-download before exact local pair removal. Default local keep count is eight; invalid/unknown/old-key artifacts remain for operator review. No remote object deletion is supported. Dry run, stale proof, remote outage, tampering/link substitution and successful retained-copy restoration pass.
- Added `scripts/lib/backup-headroom.ts`: exact filesystem-capacity accounting before snapshot creation, including WAL, conservative scratch/cache/source-growth budgets, shared-device aggregation and a256MiB reserve per filesystem. Failures do not force pruning or begin snapshots/uploads. This is a preflight, not a reservation or continuous monitor. Backup-success/retention-failure outcomes remain explicitly distinct.
- Fresh focused SQLite/retention/headroom tests pass12/12 and uncached ESLint passes. Root typecheck encountered Sol's in-flight fixture-route types; the coherent integrated run remains Sol's next checkpoint. Documentation is in `docs/38-backup-cache-retention-and-headroom.md`; the recovery and supervision runbooks are updated. Only disposable fixture copies were removed, not real backups or user files.
- Read-only Fly refresh confirms release13 and one started FRA machine. Installed secret names include no S3 backup credentials, separate backup encryption key or alert endpoint, and `flyctl storage list --org personal` returns no buckets. No provider resource, secret or production state was changed. Approved off-volume storage, external key escrow, delivered monitoring and intended-environment restore/restart evidence remain actual dependencies. Root also raised the fixed14-day bootstrap versus generic7-day A/A forecast distinction for merchant UI review; no calibration source was changed.
- Final root checkpoint: typecheck passes after the fixture types settled. The twelve focused backup tests pass again with additional corrupted-remote-copy and wrong-key no-deletion assertions; uncached test lint passes. No broader integrated or production verification is inferred from this local checkpoint.

### 2026-09-05T12:13Z — Truthful v2 timeline and durable onboarding preparation

- Added a phase-aware merchant timeline without changing the independently calibrated generic qualification estimator. Before baseline activation it states the registered56–84-day two-stage windows plus setup delay; while baseline runs it states14 enrollment +14 maturity and the later28–56-day message test; after baseline completion it reports only the message-test window. V2 never presents the stale49-day generic total, while the legacy label remains unchanged.
- Removed Shopify catalog sync, opportunity construction and Web Pixel mutation from the Overview loader. An authenticated first view now only enqueues one stable tenant/endpoint-scoped job and immediately reads presentation state. The existing five-minute offline-capability maintenance path claims the job with a bounded lease and retry budget, executes sync → deterministic preparation → Pixel setup, then records completion or a merchant/operator failure notice.
- Concurrent refreshes create one job. Completed or dead-lettered automatic work is not silently reset; an explicit Prepare action can reopen the same terminal slot with a fresh bounded attempt budget. Product choice queues its product-scoped preparation instead of blocking the action response. Endpoint changes generate a separate bounded job so a moved public origin can update the Pixel without duplicating the plan.
- Focused evidence: timeline/fixture tests14/14; onboarding worker tests2/2 cover concurrent dedupe, tenant isolation, successful catalog/plan/Pixel completion, failed retry/recovery, terminal non-resurrection and explicit retry. Typecheck and focused uncached ESLint pass. The first integrated run reached241/242 and correctly exposed a date-drift defect in a foundation test's omitted clock; root fixed that test and owns the exact final release gate.
- Changed: `app/services/autopilot-presentation.ts`, `app/services/autopilot-presentation.server.ts`, `app/services/autopilot-preparation-worker.server.ts`, `app/services/automation.server.ts`, `app/routes/app._index.tsx`, `app/routes/qa.v2-ui.tsx`, `tests/autopilot.test.ts`, `tests/autopilot-preparation-worker.test.ts` and this tracker. No production, Shopify, customer, payment, secret or commercial state changed.

### 2026-09-05 — Reproducible release gate and actual deployment contention

- Added source-manifest capture/verification and a local release command. It runs the full project check, PostgreSQL-track freshness and real isolated PostgreSQL transfer/concurrency/restore, checks independent statistical source hashes and refuses to publish a pass if source changes during the run. It records exact local scope and explicitly leaves deployment/design-partner/public readiness false. Docker context exclusions now include environment variants, local Shopify state and key/database files; an actual container build remains unverified because Docker is unavailable locally.
- The actual PostgreSQL deployment race first failed with `P2002`, reproducing a missing write lock in the empty runtime-control upsert. A nonempty self-update now serializes deployment receipt/revision reads using the same lock order as decisions. Six contested revisions return exactly one commit/one typed stale rejection each; simultaneous identical retry returns one creation and one replay. Seven exact receipts/outbox events and one product pointer are verified. Callback atomic rollback and the focused SQLite runtime/autopilot suite still pass23/23.
- Fixed a test-only clock omission in both foundation `completeJob` calls; no production lease checks were weakened. Foundation/manifest tests pass5/5 and focused lint passes. The full reproducible command then passes246/246, typecheck, full lint, app build, Shopify build, current PostgreSQL migration track, real61-table/15-seed-row/12-exclusive-job rehearsal and current independent calibration hashes. The source-bound record is `docs/release-checks/local-epFOlw/manifest.json`. Details: docs41 and42.
- This release checkpoint is local and precedes subsequent engineering changes. Root review identified asynchronous Overview progress refresh and stale/uninstalled preparation-worker mutation guards still needing implementation; these were assigned to Sol after the exact viewport QA. Actual owner/provider/Shopify/device/operational gates remain open. No production image, migration, secret, financial transaction, service purchase or commercial publication occurred.

### 2026-09-05 — Actual Fly build-only packaging checkpoint

- Ran the installed Fly CLI's documented build-only remote mode on the already authorized Pagnetic app. The actual Node22 Linux Docker build, locked pnpm install, Prisma generation, application bundle and runtime packaging passed, exporting a292MB image. The source still matches the246-test release manifest after the build. Exact base/exported image digests and reported tag are in docs43.
- Fresh post-build Fly status confirms release13, the same started FRA machine and old deployed image digest. No app deployment, production migration, secret change, Shopify extension release, paid offer or new persistent storage/database/alert service was performed. The Fly-integrated builder was used for compilation; this checkpoint is no longer merely a local Dockerfile inspection.
- A refreshed local four-tenant mixed-load fixture also passes110 operations with zero errors and decision p95 23.327ms, with4/4 delayed-delivery Original fallbacks. It does not establish cohort capacity, DB-wait/queue-worker behavior or population Core Web Vitals. Docs37/39 distinguish the dated earlier run from this refresh.

### 2026-09-05T12:58Z — Customer order privacy P0 and independent queue

- Root found a release blocker: customer data/redaction receipts discarded Shopify's supplied order IDs and claimed completion despite stored order-linked records. Intake now validates exact identifiers, encrypts only versioned tenant/order scope with the independent field key, creates one replay-safe pending receipt/deadline and deduplicated merchant notice, and sends missing scope to review. It never recreates an uninstalled merchant or claims delivery/erasure.
- Added independent privacy queue primitives:20-candidate bound,60-second conditional leases,10-attempt limit, exponential retry, deadline/scope escalation and stale authority rejection. Strict scope reads forbid plaintext compatibility, wrong keys/tenants and noncanonical/empty scopes. There is no generic completion operation and no fulfillment scheduler integration yet.
- Shop redaction now keeps merchant/session deletion and its receipt in one transaction. Its status explicitly remains `ACTIVE_DATA_DELETED_BACKUP_REVIEW`, with retained privacy records and unverified backup erasure; it has no completion timestamp. A forced receipt-insert failure proves merchant/financial deletion rolls back.
- Additive SQLite migration18 and the separate PostgreSQL track now contain62 tables; canonical schema SHA256 is `9af23d4778c2d7d84575f097a218405a2223d363d6185c3bf341f4e1bab3c21c`. Actual PostgreSQL transfer/dump/restore and two-client intake/claim/reclaim contention pass; focused privacy/foundation tests pass10/10 and focused uncached lint/typecheck pass. The earlier246-test/full-container checkpoint predates this source and is historical only.
- Required next engineering: exact customer erasure and owner-only export/delivery, transactional replay suppression across all writers, off-volume erasure journal/restore reapplication, retention/legacy-receipt review and operator status. The suppression table is storage only, not an active guard. See docs44. No live customer deletion, migration, deployment, payment or commercial change occurred; the goal remains active.
- Sol's independent review found the operations loader spreading full privacy rows into browser data. Root restricted both database selection and presentation to six explicit status/date fields; ciphertext, subject/shop hashes, lease tokens and idempotency keys never enter this summary. Regression checks enforce the exact field set. The view now shows deadlines and does not equate an empty list with compliance readiness.

### 2026-09-05T13:17Z — Transactional order replay suppression

- Added exact encrypted-scope/lease-authorized suppression staging in100-order batches, skipped-batch rejection, repeated-batch idempotency and post-write authority verification. Guarded v2 webhook intake, final canonical financial writes, legacy order/refund projection and checkout Pixel writes with the same runtime-lock-before-suppression ordering. A delayed fetched order cannot create a revision, ledger or attribution after suppression; authenticated suppressed deliveries acknowledge204 and stop retrying. Suppression survives reinstall without blocking another tenant.
- Integrated `pnpm test` passes267/267; typecheck and focused uncached lint pass. Actual62-table PostgreSQL transfer/restore and financial/lifecycle/report/deployment contention pass. Additional actual PostgreSQL suppression replay/lookup evidence is included in the current rehearsal. Synthetic zero/alphabetic order IDs were corrected to valid positive Shopify resource IDs. The financial race now intercepts before the new shared lock; awaiting a competing commit while artificially holding that lock was an impossible ordering, not a reason to remove the guard.
- Sol independently reviewed the common lock order and passed26 targeted tests. It identified the next required privacy work: durable versioned lookup-key lifecycle (API-secret rotation currently hides tombstones), exact inbox lease CAS inside canonical writes, per-request suppression journal/progress and live-clock worker orchestration. Full linked-record erasure, encrypted owner delivery and restore reapplication still do not exist; no launch/fulfillment claim is made.
- Sol also closed the previously found onboarding staging gap: each bounded local catalog/evidence/candidate/diagnosis/draft/plan/Pixel write unit rechecks runtime and exact job lease inside its transaction, with Shopify network calls outside. Root inspected the transaction boundaries; the integrated suite includes the new rollback/lease-loss fixtures. No real customer records, production migration/deployment, billing or commercial publication changed. Goal turn classification: engineering progress; goal remains active.

### 2026-09-05 — Durable privacy key lifecycle and canonical inbox authority

- Root removed implicit production dependence on Shopify API credentials for privacy lookup. Independent active/previous keys, per-record key fingerprints, global unknown/null-fingerprint fail-closed checks and historical request-key resolution preserve tombstones across API/key rotation. Migration19 is additive; real PostgreSQL transfer/restore passes on the separately reviewed62-table track. Production configuration now reports the independent key as required; no live secret changed.
- A one-request legacy adoption tool defaults to read-only verification. It proves the supplied key against authenticated original tenant/order scope and checks referenced tombstones in500-row reads. Explicit application changes fingerprints only, preserves status/data and rolls back on any mismatch. Null/orphaned/unreadable legacy scope cannot silently bypass protection. Required escrow/upgrade procedure is docs45.
- Suppression staging now persists per-request scope hash/key/progress evidence even when another request already owns the shared tombstone. Terminal cursor replay verifies prior rows, skipped gaps fail, and the record explicitly retains `backupJournalVerified=false`. No fulfillment/completion claim follows from staging.
- Sol added transactional inbox authority before canonical financial writes and in the final pre-commit check. Root reviewed the shared-lock/callback boundaries. A simulated expiry during reconciliation rolls back immutable revisions/ledger to zero; replacement recovery writes once. Network fetches remain outside transactions.
- Fresh integrated tests pass272/272; latest privacy/key/inbox tests18/18, typecheck, focused uncached lint and actual PostgreSQL transfer/dump/restore/concurrency pass. The prior246-test/container release remains historical. Full customer graph erasure, encrypted owner delivery, independent off-volume journal/restore reapplication, production setup and real Shopify evidence remain required. No production/customer/commercial state changed; this turn is engineering progress, not a blocked wait.

### 2026-09-05 — Independent audit corrections and migration20 checkpoint

- Independent review reproduced privacy receipt duplication on legitimate key rotation. Intake now serializes on a stable public-store-digest lock, independent of merchant existence/key version, searches all retained-key identities and preserves the original receipt. New Shopify data-request IDs bind stable identity; changed subject/order scope conflicts. An older process missing newer fingerprints fails closed rather than creating a competing receipt. Actual PostgreSQL concurrent cross-key intake without a merchant verifies one receipt.
- Legacy adoption now proves every expected redaction tombstone, including shared rows, instead of checking only rows carrying the current request ID. Missing/corrupt scope refuses adoption and rolls back; unstaged legacy requests require authorized staging, not a blind metadata rewrite. New shop-redaction receipts now carry the lookup-key fingerprint too. Key pruning remains prohibited without authenticated full inventory.
- Migration20 adds only `PrivacyIntakeLock`; canonical schema SHA256 `4e446ae480d34b44c953463689067cf9bc6931ddad7dd1a3674dc5449021bd59`. The separate PostgreSQL migration was reviewed locally and actual63-table transfer/dump/restore/concurrency passes. No real database migration occurred.
- Final current integrated `pnpm test` passes274/274, typecheck and focused uncached lint pass. Docs39/44/45 distinguish this source from the historical246-test/full-container checkpoint. Goal remains active: full scoped erasure, encrypted owner data delivery, journal/restore reapplication and real deployment/Shopify gates remain unverified. This is concrete engineering progress, not completion or a blocked wait.

### 2026-09-05 — Scheduled encrypted data copies and verified owner access

- Sol implemented the exact tenant/order-scoped export collector across legacy/v2 ledgers, immutable financial history and direct measurement links. Unrelated shared-assignment order values, signed capabilities and credentials are excluded. Cursor500/ID100 reads have a cumulative2000-row/4 MiB source cap and1 MiB canonical cap; over-limit requests enter explicit review, not truncated success or endless retries.
- Root added atomic one-order encrypted artifact/cursor commits, exact pre/post lease authority, progress retry reset, seven-day/deadline expiry and bounded purge. Independent five-minute `/internal/privacy` processing continues without an installed merchant or Shopify offline session. Persistent deadline/export/owner-delivery/erasure attention remains non-green; summaries contain aggregate flags/counts only.
- Owner review/download routes require both an existing app OWNER and fresh Shopify-confirmed account-owner authority for the authenticated token subject, rejecting staff/collaborator/mismatched/expired/provider-error responses. The online exchange result is discarded except for short-lived identity authority; no new session, name/email or access token is persisted. Complete contiguous unexpired artifact sets, encryption integrity and original scope binding are verified. Each download is audited; delivery and completion remain explicitly false.
- Independent review identified and root/Sol corrected false-green persistent review, skipped crypto checks when the queue became empty and bootstrap-owner privilege leakage. Migration22 stamps original storage-key fingerprints; retained history preserves replay, continued processing and access after rotation. Missing/unknown/null keys fail closed even for ready/review scopes. Authenticated one-request legacy adoption stamps storage identity without rewriting valid lookup hashes or mutating dry runs.
- Current SQLite history22/64 tables and canonical SHA256 `df55c992007dc8aac2e159506a8015502cdc1b7e0764dca6ef8281777791520c`. Separate PostgreSQL migrations21/22 were reviewed locally. Actual transfer/dump/restore and concurrency pass; two clients sharing an export lease produce exactly one first chunk, then one second chunk, with attempts reset and absent merchant still absent. No production migration/configuration/deployment occurred.
- Fresh integrated tests pass295/295; typecheck, focused uncached lint and production app build pass. The old246-test source-bound/full-container checkpoint remains historical, not certification of this source. Required next work: full erasure graph (including retained export copies), identity replay suppression, analysis invalidation, off-volume journal/restore application, delivery/backup review resolution and actual Shopify owner/download/storefront tests. Docs46 records the contract and limitations. Goal remains active; this turn made engineering progress.
### 2026-09-05T15:34Z — Privacy erasure, replay suppression and recovery-journal checkpoint

- Completed the bounded customer-redaction worker path: exact order financial scope is discovered and deleted, linked evaluation/measurement rows are removed, keyed order/identity replay suppressions are staged, analysis is invalidated, retained encrypted copies are revoked where their authenticated scope intersects, and operational references are scrubbed. The terminal state is intentionally `ACTIVE_DATA_ERASED_BACKUP_REVIEW`; the worker never claims backup deletion or legal completion.
- Added privacy graph closure over linked visitor/session/client/decision/assignment/checkout/event identities with tenant and shared-identity guards. Stale leases cannot stage suppression, graph artifacts or deletion. Redaction and graph review codes are bounded in the queue and do not retry forever or report a false green.
- Added migration `20260905230000_privacy_identity_and_analysis` for identity tombstones and experiment privacy invalidation. Current Prisma schema source SHA-256 is `4acdd4ccbe594f671b3c4fefda658581a49fdb784df7cc855657473044cf8380`; the separate PostgreSQL track is regenerated and reports no schema drift, but neither track has been applied to production.
- Integrated an encrypted, bounded suppression journal into SQLite backup creation and restore. The sidecar is remote-readback verified, authenticated with the independent backup key, excluded from plaintext manifests, retained/pruned with its database artifact, and re-applied idempotently before a restored database is copied into place. A tamper/row-count/restore-order regression is covered; this is still not evidence of a real production bucket, escrow or incident drill.
- Fresh evidence: full `pnpm test` passes303/303; typecheck, lint, production React Router build and Node24 Shopify app build pass; `pnpm exec tsx scripts/prepare-postgres-track.ts` reports canonical/track parity. The isolated PostgreSQL rehearsal now transfers 15 fixture rows across65 tables and has two workers claim12 jobs exactly once; evidence is [postgres-rehearsal.json](./audit-2026-09-05/postgres-rehearsal.json). Focused privacy/erasure/worker/journal/backup tests pass22/22. No Fly deployment, migration, secret change, customer data change, external backup upload, or Shopify live verification occurred.
- Remaining launch gates are unchanged: deploy this source only after owner-approved Fly release/migration, independently escrowed lookup/storage/backup keys, real off-volume S3-compatible upload/readback/restore, alert destination and scheduler verification, managed PostgreSQL before unattended multi-store use, and real published-theme/pixel/consent/refund/pause/uninstall evidence with a design partner. Public launch remains unclaimed.

### 2026-09-05 — Independent privacy receipt replay and quarantine correction

- The interrupted receipt/hold edits are now bounded by a v2 encrypted receipt contract. Receipts carry a backup-key semantic scope digest (so retained lookup-key rotation does not create a false conflict), an installation-generation marker for SHOP_REDACT, immutable put/readback verification, and complete S3 inventory traversal with cursor-cycle, duplicate, truncation and two-pass inventory-change detection. Existing v1 receipt objects are an explicit schema blocker, not silently upgraded.
- Added `app/services/privacy-recovery.server.ts` and `scripts/replay-privacy-receipts.ts`. The new CLI requires a NEW restored database containing `_PagneticRecoveryHold`, authenticated retained receipt-key history and a stable complete inventory; it reconstructs only exact encrypted order scopes and drives the existing leased erasure steps. Customer data-copy requests, absent historical keys, expired deadlines, scope conflicts, reinstall-generation mismatches and missing/corrupt receipts remain review-required. The isolated T0 backup → failed T1 local intake after durable receipt → held restore → authenticated inventory replay test now proves exact target erasure, original deadline retention, unrelated tenant/order preservation, startup/PG-transfer blocking, and hold persistence; failure-path tests cover missing keys/corrupt inventory. Focused receipt/recovery/backup/startup tests pass 23/23, with typecheck, targeted lint and production app build passing. The hold is deliberately retained; no backup erasure, delivery, legal completion or automatic release is claimed.
- Restore evidence now validates the signed encrypted privacy sidecar's local size/checksum/readback timestamp when present. `docs/28-encrypted-backup-and-recovery.md` now requires exact-prefix list permission, receipt inventory/backfill gates and explicit hold-release authority. At this historical checkpoint, audited hold release and uninstalled-store retrieval/delivery were still code gaps; the later17:24Z checkpoint resolves those local paths. Backup-erasure/retained-aggregate review remains external. This package itself performed no migration, deployment, provider upload or customer operation; full `pnpm test` passed311/311 before its additional isolated integration test.

### 2026-09-05T17:24Z — Audited reopening, reinstall delivery and final local release gate

- Closed the interrupted recovery-reopening engineering gap. Restored databases are quarantined by an authenticated hold; fresh databases start, while held, legacy, malformed, duplicate, unsigned or extra hold state fails closed. Receipt replay now reconstructs both exact redaction and customer data-copy work. The protected database/schema fingerprint is deterministic and bounded; operational references are included in erasure verification without requiring `completedAt` or claiming legal fulfillment.
- Added one shared testable recovery-release transaction used by the CLI. It locks the READY hold, rechecks the exact signed evidence pointer, nonfuture/fresh ordered evidence, restore/source/schema/data identity, complete stable receipt inventory, retained key fingerprints and current erasure graph, then appends one signed audit and changes the hold to signed RELEASED atomically. Startup and SQLite→PostgreSQL transfer verify the signed hold/evidence/audit chain rather than trusting a status string. Second restore cycles reset current authority while preserving append-only historical evidence.
- Completed the local post-uninstall customer-data-copy path. Encrypted v2 scope binds the original installation generation. Fresh Shopify account-owner verification permits request-specific access after a genuine reinstall without recreating an app role; same-generation no-role access remains denied. Explicit delivery confirmation requires every current part downloaded by the same verified owner and appends one signed tenant-independent audit. It stores only a digest of the external evidence reference and keeps backup/legal review false/open.
- Changed recovery/privacy source: `app/services/recovery-reopening.server.ts`, `recovery-release.server.ts`, `recovery-hold.server.ts`, `privacy-recovery.server.ts`, `privacy-installation-generation.server.ts`, `customer-privacy-scope.server.ts`, `customer-privacy-queue.server.ts`, `customer-privacy-access.server.ts`, `app/routes/app.privacy.$requestId.tsx`, `scripts/lib/sqlite-backup.ts`, `scripts/lib/postgres-transfer.ts`, `scripts/replay-privacy-receipts.ts`, `scripts/release-recovery-hold.ts`, Prisma schema/migrations and focused recovery/privacy tests. Detailed contract: docs47.
- Current identity:25 SQLite migrations; Prisma schema SHA256 `a64d9a707d974d867772b9d512978f850c28783aa19b577699d6281d745ffcfd`. The actual PostgreSQL16.15 rehearsal passes69 tables,15 seeded rows,12 exclusive claims, deployment/lifecycle/financial/privacy/report concurrency and dump/restore; `fullApplicationRtoVerified` remains false.
- Current source-bound Node24.19.0 gate passes322/322 tests, typecheck, full lint, React Router production build, Shopify app build, current PostgreSQL track/rehearsal and current statistical hashes. Manifest: `docs/release-checks/local-rvqmid/manifest.json`;315 source files; source SHA256 `f8d8b17bd675c21b086d58438c9a1e01a332dfce5beef78cc97d8d127d071787`. `pnpm check:partner` passes31/31. Local `pnpm check:production` fails exactly16 environment/external checks and therefore correctly withholds production readiness.
- Consolidated R01–R14, S01–S10 and AT01–AT30 audit found no additional ordinary safe code-controlled P0 after this checkpoint. Entries remain IN_PROGRESS where their acceptance definition includes real Shopify/browser/provider/human evidence. No Fly/Shopify deployment, production migration, secret, customer record, bucket, alert, billing, plan, charge or commercial publication changed.

### 2026-09-05T17:49Z — Astra final acceptance corrections A01–A03

- A01: recovery now recognizes `OWNER_CONFIRMED_SECURE_DELIVERY` only after authenticating the exact retained request scope/key and signed request/shop/actor/part-count/timestamp delivery audit. It returns `data-copy-delivery-authenticated` without recollection or deadline extension. Actual backup→restore→receipt replay passes after the artifact is expired and purged; missing/tampered audit fails promptly. Astra's exact reproduction now succeeds instead of reaching `PRIVACY_RECOVERY_STEP_LIMIT`.
- A02: recovery metadata verification uses a bounded active/previous backup-key ring. New artifacts still use only the active key; historical manifests, holds, replay evidence and release audits select and authenticate their exact retained key. A released database starts and reaches PostgreSQL transfer after active-key rotation with the old key retained; missing or malformed history fails closed.
- A03: restore now exclusively preserves the exact downloaded verified manifest as `<NEW.sqlite>.verified-manifest.json`, prints the path and uses the same file directly in replay/release commands. An existing manifest is never overwritten; collision rolls back only the database created by that restore invocation.
- Focused recovery/privacy/backup/access regressions pass25/25; the exact A01 reproduction prints authenticated terminal delivery. Final Node24.19.0 source-bound gate passes323/323 tests, typecheck, full lint, React Router and Shopify builds, current PostgreSQL69-table rehearsal and statistical hashes. Manifest: `docs/release-checks/local-dLdHFh/manifest.json`;315 files; source SHA256 `875470a389d874efc4d2137c9c652523517106a2e0582f6bea37da4e75fc7275`.
- No live/provider/customer state changed. Fly remains release13 on the old image with `/healthz` passing; the independent backup/privacy/S3/alert configuration needed by this source is still absent. All other local acceptance evidence stands; design-partner and public readiness remain blocked only on the documented external/human gates.

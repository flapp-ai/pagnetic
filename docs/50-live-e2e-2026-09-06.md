# Live end-to-end test — 2026-09-06

## Decision

Postdeployment browser refresh on the compatible26-migration backend: the Complete Snowboard page renders Original; native Add to cart and Buy it now are enabled. DOM inspection finds exactly one hidden `adaptive-panel-v2` block with `mode=autopilot` and all three adaptive scripts from `pagnetic-v2-reviewed-20260906`. No error-level console entries were captured. This proves disabled-v2 storefront preservation, not active treatment or measurement. Current visible product description is only a short promotional sentence ("This PREMIUM snowboard is so SUPER DUPER awesome!"); fresh source-grounded preparation may correctly abstain. Do not repurpose stale facts or invent benefits to force activation. No cart/order/theme mutation occurred during this refresh.

Latest compatibility checkpoint,07:23Z: saved block mode was subsequently changed to `autopilot` and saved/observed on the live product. Backend `reviewed-65f2cb62a661` is now deployed with26 migrations and preserved legacy approval hashes; v2/billing remain false. References to old extension/mode/release below are historical checkpoints, not current state. Fresh-plan cutover and authenticated activation evidence remain incomplete; docs26 and docs51 contain the latest evidence.

Subsequent release checkpoint: Shopify version `pagnetic-v2-reviewed-20260906` was created, validated and released under existing deployment authorization. Version ID`1117556736001`, app`418274574337`, organization`233754070`. Live test product now loads all three adaptive assets from this version and exactly one `data-block-version=adaptive-panel-v2` block. Existing saved mode is still `experiment`, not `autopilot`; panel remains hidden and Original is preserved with backend v2 disabled. This resolves the old-extension blocker below but NOT active v2 journey acceptance. No App Store listing, new commercial offer, legal approval or billing activation occurred.

PARTIAL PASS, not complete v2 launch acceptance. The deployed legacy shopper purchase/refund path works. The current Shopify extension is still `pagnetic-split-latency-20260905` and v2 is disabled; these checks cannot certify the new v2 measurement/reporting lifecycle. No feature flags, theme publication, billing or legal approvals were changed during this run.

## Verified this run

- Exact source identity still matches `docs/release-checks/local-dLdHFh/manifest.json` (`875470a389d874efc4d2137c9c652523517106a2e0582f6bea37da4e75fc7275`). Fresh `pnpm check` exit0:323/323 tests, typecheck, lint, React Router build, Shopify app/extension build. This is not a new PostgreSQL/load/statistical rehearsal.
- Fly version14, existing single FRA machine; public health reachable. Unauthenticated POSTs to internal automation/privacy both401; public support200.
- Overnight supervisor recurrence observed: backup `pagnetic-5b3b148b-cc7e-4274-9f14-27cc6c167387.sqlite.enc`, created02:34:36.092Z and restore verification02:34:37.228Z. Multiple five-minute automation/privacy requests return200 in logs through06:55Z.
- Native product page for The Complete Snowboard loads one hidden adaptive panel, Original visible, legacy adaptive/vitals CDN scripts. No duplicate panel. Desktop document width1265 within1280 viewport; captured page console has no error entries.
- Standard native add-to-cart added one Ice variant atUSD699.95; native checkout displayed the same product/variant/quantity/total.
- Checkout explicitly displayed Test Payment Gateway. Used only synthetic customer data at example.com and gateway test values. Created order `#1007`, Shopify ID`9140091912498`, confirmation`1C019MNYV`; Shopify Admin confirmed Paid and Test order. No real card, real charge or shipment.
- Full refund of that newly created test order only: USD699.95, original Bogus Gateway, quantity1 restocked, refund notification unchecked. Admin shows Refunded. Order-create/update/refund webhooks returned204.
- Direct read-only deployed DB confirms StoreOrder test=true, gross699.95, net0, currencyUSD, financialStatus=refunded. Zero experiment attribution for this Original/unassigned order. No v2 OrderLedger/inbox result is claimed: those routes are gated off.
- Variant keyboard selection changed Ice to Dawn; Buy it now handed off to checkout with Dawn, quantity1 andUSD699.95. This second checkout was not paid. Pointer radio check did not select the variant; keyboard path passed, so pointer behavior still needs controlled retest rather than a fabricated pass.
- Authenticated embedded Overview loads and preserves Original. It reports preview-only / insufficient baseline and missing shop_pay/performance evidence rather than claiming uplift.
- Embedded Results navigation succeeds and shows `No experiment yet`; no monetary uplift or synthetic test-sale benefit is asserted.

## Real isolated recovery

- Restored the overnight signed manifest and encrypted artifact from Tigris to a NEW path on encrypted Fly volume: `/data/pagnetic-e2e-recovery.YzAnzQ/restored.sqlite`. Live `/data/pilot.sqlite` was never overwritten or repointed. No plaintext DB downloaded to local Mac.
- Exact verified manifest preserved beside restored DB. Integrity and foreign-key checks passed.
- Existing replay CLI read the independent receipt store twice: inventory0, replayed0, unresolved0; evidence`18039d2b-8a62-4684-8644-a1b70abbdb41`.
- Actual app DB import with restored DATABASE_URL rejects startup with `RECOVERY_PRIVACY_REPLAY_REQUIRED`. Quarantine persists after replay pending audited release, as designed.
- Isolated restored DB remains under the private directory for follow-up. No technical release/real legal-review references were fabricated. Full application reopening/cutover, independent-host disaster recovery and measured full RTO remain unverified. Empty inventory is not evidence for post-snapshot deletion replay with real receipts.

## Exact remaining blockers / coverage gaps

### Subsequent Shop Pay applicability evidence

Read-only Shopify Admin payment settings inspection confirms the Test payment gateway is Active. Shopify Payments remains at Complete setup, and the additional-provider list is empty. The Shop Pay logo appears in the promotional list of available Shopify Payments methods, not an enabled-method confirmation. The exact gateway details state test-only transactions. Combined with the actual checkout showing only Test Payment Gateway, Shop Pay is not enabled for this current test-store configuration. Record this as configuration-specific NOT_APPLICABLE, never PASS or general Shopify compatibility. Recheck if payment-provider configuration changes; a partner with Shop Pay enabled still needs its real supported-path test. No provider, bank, two-factor or payment setting was changed.

1. Deploy the reviewed Shopify extension and prepare an explicitly test-scoped v2 configuration before testing its onboarding → approval → attribution → canonical ledger → results chain. Current backend release alone does not activate that path. Do not publish commercial offers or fabricate baseline maturity.
2. Live production readiness fails exactly two checks: `alertDeliveryConfigured` (no external destination) and `publicIdentityConfigured` (missing approved `TERMS_EFFECTIVE_DATE`; company/support/privacy/hosting already supplied). Backup proof passes.
3. Full recovery release requires actual operational/privacy/backup/legacy-inventory/quiescence review references. Do not bypass the signed hold to obtain a green startup check.
4. Shop Pay, mixed cart, selling plans where applicable, consent revoke/regrant, active v2 treatment and performance/population Web Vitals remain unverified in the intended deployment.
5. Requested390x844 viewport override did not affect observed DOM viewport (remained1280); reset override. Mobile acceptance is NOT passed on this evidence.

The source and infrastructure checks above are useful evidence, not a blanket claim that every PRD acceptance test passed or the product is launch-ready.
